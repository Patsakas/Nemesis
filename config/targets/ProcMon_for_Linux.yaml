# Auto-generated by `nemesis onboard`. Review all paths before running.
# Source scanned: /home/giorg/ProcMon_for_Linux_clean
# Build system:   cmake
# Language:       C++
#
# Oracle expansion candidates (Fix 148-150):
#   TSan candidate:  YES (threading tokens found in 2 file(s))
#                    - src/logging/easylogging++.h
#                    - src/tracer/ebpf/ebpf_tracer_engine.h
#                    → enable: set sanitizer_profile: tsan,
#                      tsan_supported: true, AND threaded_oracle: true
#                      on at least one pinned_func.
#   MSan candidate:  MAYBE (external deps need MSan-rebuild: -lstdc++)
#                    → before enabling, rebuild deps with -fsanitize=memory
#                      or accept a false-positive flood from those libs.
fuzzing:
  timeout_hours: 0.25
target:
  name: ProcMon_for_Linux
  oss_fuzz_project: ProcMon_for_Linux
  source_root: $HOME/ProcMon_for_Linux_clean
  work_root: $HOME/ProcMon_for_Linux_work
  build_dir: $HOME/ProcMon_for_Linux_work/build_fuzz
  debug_build_dir: $HOME/ProcMon_for_Linux_clean/build_debug
  ubsan_build_dir: $HOME/ProcMon_for_Linux_clean/build_ubsan
  coverage_build_dir: $HOME/ProcMon_for_Linux_clean/build_coverage
  build:
    configure: rm -f CMakeCache.txt && CC=afl-clang-fast CXX=afl-clang-fast++ cmake .. -DCMAKE_C_COMPILER=afl-clang-fast -DCMAKE_CXX_COMPILER=afl-clang-fast++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DCMAKE_CXX_FLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    make: make -j$(nproc) ProcMon_for_Linux
    debug_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DCMAKE_CXX_FLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    debug_make: make -j$(nproc) ProcMon_for_Linux
    ubsan_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DCMAKE_CXX_FLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    ubsan_make: make -j$(nproc) ProcMon_for_Linux
    coverage_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DCMAKE_CXX_FLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    coverage_make: make -j$(nproc) ProcMon_for_Linux
  repro_binary: ''
  repro_args: []
  source_subdir: ''
  include_subdir: src
  library_name: libprocmon_for_linux.a
  link_libs: -lpthread -lstdc++ -lm
  harness_includes:
  - installer.h
  harness_template: |
    You are a fuzzing engineer generating AFL++ harnesses for ProcMon_for_Linux.

    CRITICAL RULES:
    1. Feed fuzz input via in-memory buffers ONLY — this library does NOT parse untrusted file formats. Fuzz the eBPF object extraction/validation logic by providing crafted .o files as input buffers.
    2. The library operates on pre-installed eBPF programs (static .o files). There is NO handle/context object — all functions are stateless file-system operations.
    3. Mandatory init/cleanup: None. `ExtractEBPFPrograms()` and `DeleteEBPFPrograms()` are standalone and do not require pairing.
    4. Forbidden patterns: Do NOT allocate attacker-controlled sizes for file paths or buffers. All paths are hardcoded in installer.h (e.g., `/tmp`, `procmonEBPFkern*.o`).
    5. Required #include list: <installer.h>, <stdint.h>, <stdlib.h>, <string.h>.
    6. AFL++ persistent mode: Use `__AFL_FUZZ_INIT();` at file scope, `__AFL_LOOP(10000)`, and access input via `__AFL_FUZZ_TESTCASE_BUF`/`__AFL_FUZZ_TESTCASE_LEN`.
    7. Input must be treated as a raw .o file (ELF) for `ExtractEBPFPrograms()`. For `DeleteEBPFPrograms()`, input is ignored (no parsing).
    8. NEVER call internal kernel helpers (e.g., `bpf_prog_load()`, `libbpf_*`) — they are not in public headers and will fail to link.

    FORBIDDEN INTERNAL FUNCTIONS:
    NEVER call these even if you see them used inside the target function source — they will fail to link: `bpf_prog_load()`, `bpf_object__open()`, `bpf_object__load()`, `libbpf_set_print()`, `bpf_program__fd()`, `bpf_map__fd()`, `bpf_object__unload()`.

    PROVEN WORKING TEMPLATE:
    ```c
    #include <installer.h>
    #include <stdint.h>
    #include <stdlib.h>
    #include <string.h>

    __AFL_FUZZ_INIT();

    int main(int argc, char **argv) {
        __AFL_INIT();

        __AFL_LOOP(10000) {
            // Write input buffer to a temporary .o file for ExtractEBPFPrograms
            char temp_path[256];
            snprintf(temp_path, sizeof(temp_path), "/tmp/fuzz_%d.o", getpid());
            FILE *f = fopen(temp_path, "wb");
            if (f) {
                fwrite(__AFL_FUZZ_TESTCASE_BUF, 1, __AFL_FUZZ_TESTCASE_LEN, f);
                fclose(f);
                // TODO: adapt for target function (ExtractEBPFPrograms or DeleteEBPFPrograms)
                ExtractEBPFPrograms();
                remove(temp_path);
            }
        }
        return 0;
    }
    ```

    Output ONLY valid JSON with:
    { "target_func": "function_name", "input_format": "raw ELF .o file (eBPF program)", "c_code": "complete C harness source code using the template above", "seed_commands": ["echo 'ELF...' > seed.o"], "compile_flags": "-g -O1 -fno-omit-frame-pointer" }
  api_func_fixes: {}
  magic_bytes:
    ELF:
    - \x7fELF
recon_scoring:
  bonus_patterns: {}
  bonus_func_patterns:
    Extract: 15
    Delete: 10
  penalty_dirs:
  - test
  - tests
  - doc
  - docs
  - man
  penalty_files: []
  penalty_funcs: []
  low_value_files: {}
seeds:
  formats: {}
  oss_fuzz_corpus: $HOME/Nemesis/seeds/oss_fuzz_corpus_ProcMon_for_Linux
  oss_fuzz_fuzzer_names: []
