# Auto-generated by `nemesis onboard`. Review all paths before running.
# Source scanned: /home/giorg/clam_clean
# Build system:   cmake
# Language:       C++
#
# External dependencies (CMake find_package):
#   Boost              → (no apt hint; check distro packages)
#   Git                → (no apt hint; check distro packages)
#   LLVM               → (no apt hint; check distro packages)
#
# Oracle expansion candidates (Fix 148-150):
#   TSan candidate:  NO (no pthread/std::thread/OpenMP/_Atomic in source)
#   MSan candidate:  MAYBE (external deps need MSan-rebuild: -lstdc++)
#                    → before enabling, rebuild deps with -fsanitize=memory
#                      or accept a false-positive flood from those libs.
fuzzing:
  timeout_hours: 0.25
target:
  name: clam
  oss_fuzz_project: clam
  source_root: $HOME/clam_clean
  work_root: $HOME/clam_work
  build_dir: $HOME/clam_work/build_fuzz
  debug_build_dir: $HOME/clam_clean/build_debug
  ubsan_build_dir: $HOME/clam_clean/build_ubsan
  coverage_build_dir: $HOME/clam_clean/build_coverage
  build:
    configure: rm -f CMakeCache.txt && CC=afl-clang-fast CXX=afl-clang-fast++ cmake .. -DCMAKE_C_COMPILER=afl-clang-fast -DCMAKE_CXX_COMPILER=afl-clang-fast++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DCMAKE_CXX_FLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DCLAM_INCLUDE_TESTS=OFF -DCLAM_INCLUDE_TOOLS=OFF -DCLAM_BUILD_LIBS_SHARED=OFF
    make: make -j$(nproc) clam
    debug_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DCMAKE_CXX_FLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DCLAM_INCLUDE_TESTS=OFF -DCLAM_INCLUDE_TOOLS=OFF -DCLAM_BUILD_LIBS_SHARED=OFF
    debug_make: make -j$(nproc) clam
    ubsan_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DCMAKE_CXX_FLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DCLAM_INCLUDE_TESTS=OFF -DCLAM_INCLUDE_TOOLS=OFF -DCLAM_BUILD_LIBS_SHARED=OFF
    ubsan_make: make -j$(nproc) clam
    coverage_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DCMAKE_CXX_FLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DCLAM_INCLUDE_TESTS=OFF -DCLAM_INCLUDE_TOOLS=OFF -DCLAM_BUILD_LIBS_SHARED=OFF
    coverage_make: make -j$(nproc) clam
  repro_binary: ''
  repro_args: []
  source_subdir: ''
  include_subdir: include/clam
  library_name: libclam.a
  link_libs: -lpthread -lstdc++ -lm
  harness_includes:
  - clam.h
  harness_template: |
    You are a fuzzing engineer generating AFL++ harnesses for clam.

    CRITICAL RULES:
    1. Feed the entire AFL++ testcase buffer directly to the target API as an in‑memory byte array; the library does not perform file I/O, so use the raw buffer (`__AFL_FUZZ_TESTCASE_BUF`) and its length (`__AFL_FUZZ_TESTCASE_LEN`).
    2. Most public functions expect plain pointers and sizes, not a handle/context object. Pass the buffer pointer and length directly.
    3. If a function requires explicit initialization (e.g., a library‑wide init function), call it before the fuzz loop and pair it with the corresponding shutdown/cleanup after the loop. If no such API exists, no extra init/cleanup is needed.
    4. NEVER allocate attacker‑controlled memory sizes without first checking the length against a reasonable bound (e.g., `if (len > MAX_INPUT) return;`). Doing so can cause out‑of‑memory crashes that are false positives for the fuzzer.
    5. REQUIRED #include list: include the public header `clam.h` and standard headers needed for AFL (`<stddef.h>`, `<stdint.h>` if not already pulled in).
    6. AFL++ persistent mode must be used: place `__AFL_FUZZ_INIT();` at file scope right after the includes, and wrap the processing code inside `while (__AFL_LOOP(10000)) { ... }`. Use `__AFL_FUZZ_TESTCASE_BUF` and `__AFL_FUZZ_TESTCASE_LEN` to obtain the input.

    PROVEN WORKING TEMPLATE:
    ```c
    #include <stddef.h>
    #include <stdint.h>
    #include "clam.h"

    /* AFL++ persistent mode initialization – must be at file scope */
    __AFL_FUZZ_INIT();

    /* Example target function prototype – replace with the actual function you want to fuzz */
    extern void target_function(const uint8_t *data, size_t size);

    int main(int argc, char **argv) {
        /* AFL++ entry‑point initialization */
        __AFL_INIT();

        while (__AFL_LOOP(10000)) {
            const uint8_t *buf = __AFL_FUZZ_TESTCASE_BUF;
            size_t      len = __AFL_FUZZ_TESTCASE_LEN;

            /* Optional sanity check to avoid extreme sizes */
            if (len > 1024 * 1024) {
                continue; /* skip inputs larger than 1 MiB */
            }

            /* Call the library function with the fuzz data */
            target_function(buf, len);
            /* TODO: adapt for target function X – replace `target_function` with the actual API entry point */
        }
        return 0;
    }
    ```

    Output ONLY valid JSON with:
    {
      "target_func": "function_name",
      "input_format": "raw binary buffer passed to the target API",
      "c_code": "complete C harness source code using the template above",
      "seed_commands": ["# generate a minimal seed file, e.g., echo -n \"\" > seed.bin"],
      "compile_flags": "-g -O1 -fno-omit-frame-pointer"
    }
  api_func_fixes: {}
  magic_bytes: {}
recon_scoring:
  bonus_patterns: {}
  bonus_func_patterns: {}
  penalty_dirs:
  - test
  - tests
  - doc
  - docs
  - man
  penalty_files: []
  penalty_funcs: []
  low_value_files: {}
seeds:
  formats: {}
  oss_fuzz_corpus: $HOME/Nemesis/seeds/oss_fuzz_corpus_clam
  oss_fuzz_fuzzer_names: []
