# Auto-generated by `nemesis onboard`. Review all paths before running.
# Source scanned: /home/giorg/minmea_clean
# Build system:   cmake
# Language:       C
#
# External dependencies (CMake find_package):
#   PkgConfig          → (no apt hint; check distro packages)
#
# Oracle expansion candidates (Fix 148-150):
#   TSan candidate:  NO (no pthread/std::thread/OpenMP/_Atomic in source)
#   MSan candidate:  YES (no external link deps that need MSan-rebuild)
#                    → enable: set sanitizer_profile: msan and
#                      msan_supported: true. Run as a SEPARATE campaign;
#                      mutually exclusive with the default ASAN build.
fuzzing:
  timeout_hours: 0.25
target:
  name: minmea
  oss_fuzz_project: minmea
  source_root: $HOME/minmea_clean
  work_root: /home/giorg/minmea_work
  build_dir: /home/giorg/minmea_work/build_fuzz
  debug_build_dir: $HOME/minmea_clean/build_debug
  ubsan_build_dir: $HOME/minmea_clean/build_ubsan
  coverage_build_dir: $HOME/minmea_clean/build_coverage
  build:
    configure: rm -f CMakeCache.txt && CC=afl-clang-fast CXX=afl-clang-fast++ cmake .. -DCMAKE_C_COMPILER=afl-clang-fast -DCMAKE_CXX_COMPILER=afl-clang-fast++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DMINMEA_ENABLE_TESTING=OFF
    make: make -j$(nproc) minmea
    debug_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DMINMEA_ENABLE_TESTING=OFF
    debug_make: make -j$(nproc) minmea
    ubsan_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DMINMEA_ENABLE_TESTING=OFF
    ubsan_make: make -j$(nproc) minmea
    coverage_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DMINMEA_ENABLE_TESTING=OFF
    coverage_make: make -j$(nproc) minmea
  repro_binary: ''
  repro_args: []
  source_subdir: ''
  include_subdir: ''
  library_name: libminmea.a
  link_libs: -lm
  harness_includes:
  - minmea.h
  harness_template: |
    You are a fuzzing engineer generating AFL++ harnesses for minmea.

    CRITICAL RULES:
    1. Feed fuzz input as a null-terminated string buffer (NMEA sentence) via `const char *`. The library expects C-style strings.
    2. The library operates on raw in-memory buffers. No FILE* or custom callbacks are required.
    3. No explicit init/cleanup is needed for the library itself. All parsing functions are stateless.
    4. Forbidden: Do NOT pass attacker-controlled lengths to `minmea_scan` or parsing functions without ensuring null-termination. Always use `__AFL_FUZZ_TESTCASE_BUF` as a null-terminated string (AFL++ guarantees null termination for the input buffer).
    5. Mandatory includes: `#include "minmea.h"` (public header only).
    6. AFL++ persistent mode: Use `__AFL_FUZZ_INIT();` at file scope, `__AFL_LOOP(10000)` in main, and access input via `__AFL_FUZZ_TESTCASE_BUF` (null-terminated) and `__AFL_FUZZ_TESTCASE_LEN`.
    7. Always null-terminate the input buffer. AFL++ provides this by default, but if you manipulate the buffer, ensure it remains null-terminated.
    8. Target functions: Prioritize `minmea_parse_*`, `minmea_scan`, `minmea_check`, and `minmea_sentence_id` for fuzzing.
    9. Avoid functions like `minmea_getdatetime` or `minmea_gettime` as primary targets—they are secondary consumers of parsed data.
    10. Seed inputs should be valid NMEA sentences (e.g., `$GPGGA,...) or malformed variants to trigger edge cases.

    FORBIDDEN INTERNAL FUNCTIONS:
    NEVER call these even if you see them used inside the target function source — they will fail to link.

    PROVEN WORKING TEMPLATE:
    ```c
    #include "minmea.h"
    #include <string.h>

    __AFL_FUZZ_INIT();

    int main(int argc, char **argv) {
        __AFL_INIT();

        __AFL_LOOP(10000) {
            /* Input is null-terminated by AFL++ */
            const char *sentence = (const char *)__AFL_FUZZ_TESTCASE_BUF;
            size_t len = __AFL_FUZZ_TESTCASE_LEN;

            /* Ensure null-termination (AFL++ does this, but be explicit) */
            char *buf = (char *)malloc(len + 1);
            if (!buf) return 1;
            memcpy(buf, sentence, len);
            buf[len] = '\0';

            /* TODO: adapt for target function X */
            /* Example: Parse as RMC sentence */
            struct minmea_sentence_rmc frame;
            if (minmea_parse_rmc(&frame, buf)) {
                /* Valid parse - library processed it */
                volatile int dummy = frame.latitude.value; /* Prevent dead-store elimination */
            }

            /* Alternative: Use minmea_scan for generic parsing */
            /* struct minmea_float lat; */
            /* if (minmea_scan(buf, "f", &lat)) { */
            /*     volatile float f = minmea_tofloat(&lat); */
            /* } */

            /* Alternative: Check sentence validity */
            /* if (minmea_check(buf, true)) { */
            /*     volatile int id = minmea_sentence_id(buf, true); */
            /* } */

            free(buf);
        }

        return 0;
    }
    ```

    Output ONLY valid JSON with:
    { "target_func": "function_name", "input_format": "NMEA sentence string (null-terminated)", "c_code": "complete C harness source code using the template above", "seed_commands": ["echo '$GPGGA,092750.000,5321.6802,N,00630.3372,W,1,8,,100.0,M,50.0,M,,*47' > seed1.txt", "echo '$GPRMC,092750.000,A,5321.6802,N,00630.3372,W,0.00,0.00,280524,,,A*6B' > seed2.txt", "python3 -c \"import random; print('$GPGGA,' + ','.join([''.join(random.choices('0123456789.,', k=10)) for _ in range(6)]) + '*00')\" > seed3.txt"], "compile_flags": "-g -O1 -fno-omit-frame-pointer" }
  api_func_fixes: {}
  magic_bytes:
    NMEA:
    - $GP
    - $GA
    - $GL
    - $GN
    - $BD
recon_scoring:
  bonus_patterns: {}
  bonus_func_patterns:
    parse_: 15
    scan: 15
    check: 10
    sentence_id: 10
  penalty_dirs:
  - test
  - tests
  - doc
  - docs
  - man
  penalty_files: []
  penalty_funcs: []
  low_value_files: {}
seeds:
  formats: {}
  oss_fuzz_corpus: $HOME/Nemesis/seeds/oss_fuzz_corpus_minmea
  oss_fuzz_fuzzer_names: []
