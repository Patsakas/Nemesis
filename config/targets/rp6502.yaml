# Auto-generated by `nemesis onboard`. Review all paths before running.
# Source scanned: /home/giorg/rp6502_clean
# Build system:   cmake
# Language:       C
#
# External dependencies (CMake find_package):
#   OpenGL             → (no apt hint; check distro packages)
#   X11                → (no apt hint; check distro packages)
#   ALSA               → (no apt hint; check distro packages)
#
# Oracle expansion candidates (Fix 148-150):
#   TSan candidate:  YES (threading tokens found in 1 file(s))
#                    - src/emu/dbg/dbg.c
#                    → enable: set sanitizer_profile: tsan,
#                      tsan_supported: true, AND threaded_oracle: true
#                      on at least one pinned_func.
#   MSan candidate:  YES (no external link deps that need MSan-rebuild)
#                    → enable: set sanitizer_profile: msan and
#                      msan_supported: true. Run as a SEPARATE campaign;
#                      mutually exclusive with the default ASAN build.
fuzzing:
  timeout_hours: 0.25
target:
  name: rp6502
  oss_fuzz_project: rp6502
  source_root: $HOME/rp6502_clean
  work_root: $HOME/rp6502_work
  build_dir: $HOME/rp6502_work/build_fuzz
  debug_build_dir: $HOME/rp6502_clean/build_debug
  ubsan_build_dir: $HOME/rp6502_clean/build_ubsan
  coverage_build_dir: $HOME/rp6502_clean/build_coverage
  build:
    configure: rm -f CMakeCache.txt && CC=afl-clang-fast CXX=afl-clang-fast++ cmake .. -DCMAKE_C_COMPILER=afl-clang-fast -DCMAKE_CXX_COMPILER=afl-clang-fast++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DRP6502_EMU_BUILD_TESTS=OFF
    make: make -j$(nproc) emu_core
    debug_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DRP6502_EMU_BUILD_TESTS=OFF
    debug_make: make -j$(nproc) emu_core
    ubsan_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DRP6502_EMU_BUILD_TESTS=OFF
    ubsan_make: make -j$(nproc) emu_core
    coverage_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DRP6502_EMU_BUILD_TESTS=OFF
    coverage_make: make -j$(nproc) emu_core
  repro_binary: ''
  repro_args: []
  source_subdir: src/emu
  include_subdir: src/emu
  library_name: src/emu/libemu_core.a
  link_libs: -lm
  harness_includes:
  - main.h
  harness_template: |+
    You are a fuzzing engineer generating AFL++ harnesses for rp6502.

    CRITICAL RULES:
    1. Feed fuzz input via a memory-backed FILE* created from __AFL_FUZZ_TESTCASE_BUF/__AFL_FUZZ_TESTCASE_LEN inside __AFL_LOOP body. Do NOT use raw memory; the emulator expects a FILE* handle.
    2. The library requires mandatory init/cleanup pairing: call main_init() once at harness start and do NOT call any shutdown (emulator runs in persistent mode; cleanup is handled by the harness exit).
    3. Never allocate attacker-controlled sizes without internal bounds checks. The emulator’s FatFs and XREG paths perform their own size checks; do NOT bypass them.
    4. Required #include list: <stdbool.h>, <stdint.h>, "main.h".
    5. Use AFL++ persistent mode: __AFL_FUZZ_INIT() at file scope after includes, __AFL_LOOP(10000) in main, and read input via __AFL_FUZZ_TESTCASE_BUF/__AFL_FUZZ_TESTCASE_LEN inside the loop.
    6. The emulator expects a valid RIA filesystem image or raw data stream; do NOT feed zero-length inputs.
    7. main_dir_ops_set() must be called with false to route I/O to the emulator’s host handlers (avoids FatFs path false positives).
    8. main_xreg_0() and main_xreg_1() are the primary device dispatchers; fuzz their channel, address, and word parameters.
    9. Do NOT call any internal helpers (none exposed in public headers).
    10. Compile with -g -O1 -fno-omit-frame-pointer; no library linking flags needed (static archive already provided).

    PROVEN WORKING TEMPLATE:
    ```c
    #include <stdbool.h>
    #include <stdint.h>
    #include "main.h"

    __AFL_FUZZ_INIT();

    int main(int argc, char **argv) {
        __AFL_INIT();

        main_init();
        main_dir_ops_set(false);

        while (__AFL_LOOP(10000)) {
            if (__AFL_FUZZ_TESTCASE_LEN < 1) continue;

            /* Build a memory-backed FILE* from AFL input */
            FILE *f = fmemopen((void *)__AFL_FUZZ_TESTCASE_BUF, __AFL_FUZZ_TESTCASE_LEN, "rb");
            if (!f) continue;

            /* TODO: adapt for target function: feed data via XREG or filesystem emulation */
            /* Example: fuzz main_xreg_0() device 0 */
            uint8_t channel = 0;
            uint8_t address = 0;
            uint16_t word = 0;

            /* Feed attacker-controlled bytes into channel/address/word */
            if (__AFL_FUZZ_TESTCASE_LEN >= 1) channel = ((uint8_t *)__AFL_FUZZ_TESTCASE_BUF)[0] & 0x0F;
            if (__AFL_FUZZ_TESTCASE_LEN >= 2) address = ((uint8_t *)__AFL_FUZZ_TESTCASE_BUF)[1] & 0xFF;
            if (__AFL_FUZZ_TESTCASE_LEN >= 3) word = ((uint16_t *)__AFL_FUZZ_TESTCASE_BUF)[1] & 0xFFFF;

            bool ok = main_xreg_0(channel, address, word);
            (void)ok;

            fclose(f);
        }

        return 0;
    }
    ```

    Output ONLY valid JSON with:
    {
      "target_func": "function_name",
      "input_format": "rp6502-specific input description",
      "c_code": "complete C harness source code using the template above",
      "seed_commands": ["shell commands to generate seed files"],
      "compile_flags": "-g -O1 -fno-omit-frame-pointer"
    }

  api_func_fixes: {}
  magic_bytes: {}
recon_scoring:
  bonus_patterns: {}
  bonus_func_patterns:
    xreg_: 12
    main_: 8
  penalty_dirs:
  - test
  - tests
  - doc
  - docs
  - man
  penalty_files: []
  penalty_funcs: []
  low_value_files: {}
seeds:
  formats: {}
  oss_fuzz_corpus: $HOME/Nemesis/seeds/oss_fuzz_corpus_rp6502
  oss_fuzz_fuzzer_names: []
