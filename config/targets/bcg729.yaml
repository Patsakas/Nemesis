# Auto-generated by `nemesis onboard`. Review all paths before running.
# Source scanned: /home/giorg/bcg729_clean
# Build system:   cmake
# Language:       C
#
# Oracle expansion candidates (Fix 148-150):
#   TSan candidate:  NO (no pthread/std::thread/OpenMP/_Atomic in source)
#   MSan candidate:  YES (no external link deps that need MSan-rebuild)
#                    → enable: set sanitizer_profile: msan and
#                      msan_supported: true. Run as a SEPARATE campaign;
#                      mutually exclusive with the default ASAN build.
fuzzing:
  timeout_hours: 0.25
target:
  name: bcg729
  oss_fuzz_project: bcg729
  source_root: $HOME/bcg729_clean
  work_root: $HOME/bcg729_work
  build_dir: $HOME/bcg729_work/build_fuzz
  debug_build_dir: $HOME/bcg729_clean/build_debug
  ubsan_build_dir: $HOME/bcg729_clean/build_ubsan
  coverage_build_dir: $HOME/bcg729_clean/build_coverage
  build:
    configure: rm -f CMakeCache.txt && CC=afl-clang-fast CXX=afl-clang-fast++ cmake .. -DCMAKE_C_COMPILER=afl-clang-fast -DCMAKE_CXX_COMPILER=afl-clang-fast++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DENABLE_UNIT_TESTS=OFF
    make: make -j$(nproc) bcg729
    debug_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DENABLE_UNIT_TESTS=OFF
    debug_make: make -j$(nproc) bcg729
    ubsan_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DENABLE_UNIT_TESTS=OFF
    ubsan_make: make -j$(nproc) bcg729
    coverage_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DENABLE_UNIT_TESTS=OFF
    coverage_make: make -j$(nproc) bcg729
  repro_binary: ''
  repro_args: []
  source_subdir: src
  include_subdir: include/bcg729
  library_name: src/libbcg729.a
  link_libs: -lm
  harness_includes:
  - decoder.h
  harness_template: |+
    You are a fuzzing engineer generating AFL++ harnesses for bcg729.

    CRITICAL RULES:
    1. bcg729 operates on raw in-memory buffers: the fuzz input is a G.729 bitstream (80 bits per frame, 10 bytes). Feed the AFL buffer directly to bcg729Decoder via the bitStream parameter.
    2. bcg729 requires a decoder context object created by initBcg729DecoderChannel() and freed by closeBcg729DecoderChannel(). Mandatory init/cleanup pairing: every init must have a matching close.
    3. bcg729Decoder expects bitStreamLength in bytes (typically 10 for a full frame). Do NOT pass attacker-controlled sizes without internal bounds checks; always clamp bitStreamLength to the actual input length.
    4. Required #include list: <stdint.h>, "decoder.h".
    5. bcg729 has no internal headers; all public API is in decoder.h.
    6. Forbidden patterns: never call closeBcg729DecoderChannel() on a NULL pointer; always check initBcg729DecoderChannel() return value.
    7. bcg729 is a pure decoder; it does not perform I/O. Use the AFL buffer directly; no FILE*, sockets, or custom callbacks are needed.
    8. AFL++ persistent mode: use __AFL_FUZZ_INIT() at file scope, __AFL_LOOP(10000), __AFL_FUZZ_TESTCASE_BUF, __AFL_FUZZ_TESTCASE_LEN inside __AFL_LOOP body.
    9. bcg729Decoder writes 80 int16_t samples (160 bytes) to the caller-allocated signal[] array. Ensure signal buffer is large enough (>= 160 bytes) to avoid OOB writes.
    10. bcg729Decoder flags (frameErasureFlag, SIDFrameFlag, rfc3389PayloadFlag) should be set conservatively during fuzzing to explore different modes; default to 0 for normal frames.

    PROVEN WORKING TEMPLATE:
    ```c
    #include <stdint.h>
    #include "decoder.h"
    __AFL_FUZZ_INIT();

    int main(int argc, char **argv) {
        __AFL_INIT();

        bcg729DecoderChannelContextStruct *ctx = NULL;
        int16_t signal[80];

        while (__AFL_LOOP(10000)) {
            size_t len = __AFL_FUZZ_TESTCASE_LEN;
            if (len < 10) continue; /* G.729 frame is 10 bytes; skip undersized */

            ctx = initBcg729DecoderChannel();
            if (!ctx) continue; /* allocation failure */

            /* TODO: adapt for target function bcg729Decoder */
            bcg729Decoder(ctx, __AFL_FUZZ_TESTCASE_BUF, (uint8_t)len, 0, 0, 0, signal);

            closeBcg729DecoderChannel(ctx);
            ctx = NULL;
        }

        return 0;
    }
    ```

    Output ONLY valid JSON with:
    {
      "target_func": "function_name",
      "input_format": "bcg729-specific input description",
      "c_code": "complete C harness source code using the template above",
      "seed_commands": ["shell commands to generate seed files"],
      "compile_flags": "-g -O1 -fno-omit-frame-pointer"
    }

  api_func_fixes: {}
  magic_bytes:
    G.729:
    - \x62\x00\x00\x00\x00\x00\x00\x00\x00\x00
recon_scoring:
  bonus_patterns: {}
  bonus_func_patterns:
    bcg729Decoder: 15
    initBcg729: 10
    closeBcg729: 10
  penalty_dirs:
  - test
  - tests
  - doc
  - docs
  - man
  penalty_files: []
  penalty_funcs: []
  low_value_files: {}
seeds:
  formats: {}
  oss_fuzz_corpus: $HOME/Nemesis/seeds/oss_fuzz_corpus_bcg729
  oss_fuzz_fuzzer_names: []
