# Auto-generated by `nemesis onboard`. Review all paths before running.
# Source scanned: /home/giorg/vdi_stream_client_clean
# Build system:   autoconf
# Language:       C
#
# Oracle expansion candidates (Fix 148-150):
#   TSan candidate:  YES (threading tokens found in 4 file(s))
#                    - src/input.c
#                    - src/parsec.h
#                    - src/ffmpeg.c
#                    + 1 more
#                    → enable: set sanitizer_profile: tsan,
#                      tsan_supported: true, AND threaded_oracle: true
#                      on at least one pinned_func.
#   MSan candidate:  YES (no external link deps that need MSan-rebuild)
#                    → enable: set sanitizer_profile: msan and
#                      msan_supported: true. Run as a SEPARATE campaign;
#                      mutually exclusive with the default ASAN build.
fuzzing:
  timeout_hours: 0.25
target:
  name: vdi_stream_client
  oss_fuzz_project: vdi_stream_client
  source_root: $HOME/vdi_stream_client_clean
  work_root: $HOME/vdi_stream_client_work
  build_dir: $HOME/vdi_stream_client_work/build_fuzz
  debug_build_dir: $HOME/vdi_stream_client_clean/build_debug
  ubsan_build_dir: $HOME/vdi_stream_client_clean/build_ubsan
  coverage_build_dir: $HOME/vdi_stream_client_clean/build_coverage
  build:
    configure: '{ (cd .. && make distclean) >/dev/null 2>&1; true; }; rm -f ../config.status ../config.log >/dev/null 2>&1 || true; [ -x ../configure ] || (cd .. && (./autogen.sh --help >/dev/null 2>&1 && ./autogen.sh --without-python || autoreconf -fi)) && CC=afl-clang-fast CXX=afl-clang-fast++ CFLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" CXXFLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" ../configure --enable-static --disable-shared --without-python'
    make: make -j$(nproc) -C src libvdi_stream_client.la
    debug_configure: '{ (cd .. && make distclean) >/dev/null 2>&1; true; }; rm -f ../config.status ../config.log >/dev/null 2>&1 || true; [ -x ../configure ] || (cd .. && (./autogen.sh --help >/dev/null 2>&1 && ./autogen.sh --without-python || autoreconf -fi)) && CC=clang CXX=clang++ CFLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" CXXFLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" ../configure --enable-static --disable-shared --without-python'
    debug_make: make -j$(nproc) -C src libvdi_stream_client.la
    ubsan_configure: '{ (cd .. && make distclean) >/dev/null 2>&1; true; }; rm -f ../config.status ../config.log >/dev/null 2>&1 || true; [ -x ../configure ] || (cd .. && (./autogen.sh --help >/dev/null 2>&1 && ./autogen.sh --without-python || autoreconf -fi)) && CC=clang CXX=clang++ CFLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" CXXFLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" ../configure --enable-static --disable-shared --without-python'
    ubsan_make: make -j$(nproc) -C src libvdi_stream_client.la
    coverage_configure: '{ (cd .. && make distclean) >/dev/null 2>&1; true; }; rm -f ../config.status ../config.log >/dev/null 2>&1 || true; [ -x ../configure ] || (cd .. && (./autogen.sh --help >/dev/null 2>&1 && ./autogen.sh --without-python || autoreconf -fi)) && CC=clang CXX=clang++ CFLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" CXXFLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" ../configure --enable-static --disable-shared --without-python'
    coverage_make: make -j$(nproc) -C src libvdi_stream_client.la
  repro_binary: ''
  repro_args: []
  source_subdir: src
  include_subdir: include
  library_name: .libs/libvdi_stream_client.a
  link_libs: -lm
  harness_includes:
  - vdi_stream_client.h
  harness_template: |
    You are a fuzzing engineer generating AFL++ harnesses for vdi_stream_client.

    CRITICAL RULES:
    1. Feed fuzz input to the library via an in-memory buffer provided by AFL++ macros: use __AFL_FUZZ_TESTCASE_BUF and __AFL_FUZZ_TESTCASE_LEN inside the __AFL_LOOP body. Do NOT use FILE* or custom callbacks unless the API explicitly requires thandle_t with memory-backed callbacks.
    2. The library exposes a public API via vdi_stream_client.h (not provided here). All API calls require a valid client handle obtained from vdi_stream_client_init() and released with vdi_stream_client_shutdown(). Mandatory init/cleanup pairing must be respected in every harness.
    3. Never allocate attacker-controlled sizes without internal bounds checks. If the API exposes a function that accepts a user-controlled size (e.g., vdi_stream_client_read()), ensure the harness bounds the read to the AFL input length to avoid OOB reads.
    4. Required #include list: <stdint.h>, <stdlib.h>, <string.h>, "vdi_stream_client.h".
    5. Use AFL++ persistent mode: __AFL_FUZZ_INIT() at file scope immediately after includes, __AFL_INIT() in main, and __AFL_LOOP(10000) in the fuzzing loop. Use __AFL_FUZZ_TESTCASE_BUF and __AFL_FUZZ_TESTCASE_LEN inside the loop body.
    6. The library is not a file-format parser; magic_bytes are irrelevant. Focus on exercising client initialization, stream operations, and shutdown.
    7. Forbidden patterns: calling vdi_stream_client_init() without vdi_stream_client_shutdown() in all code paths; passing NULL or uninitialized handles to API functions; using attacker-controlled sizes without clamping to input length.
    8. If the public API exposes a thandle_t callback interface (e.g., for custom I/O), implement memory-backed callbacks (read/seek/close) that read from the AFL buffer. Do NOT stub them with NULL.
    9. Prefer short-lived client handles per fuzz iteration to maximize coverage and reduce state leakage. Avoid global state across iterations.
    10. If the API exposes vdi_stream_client_version() or similar metadata helpers, they are low-value fuzz targets; prioritize functions that parse or process streams (e.g., vdi_stream_client_read(), vdi_stream_client_seek()).

    PROVEN WORKING TEMPLATE:
    ```c
    #include <stdint.h>
    #include <stdlib.h>
    #include <string.h>
    #include "vdi_stream_client.h"

    __AFL_FUZZ_INIT();

    int main(int argc, char **argv) {
        __AFL_INIT();

        unsigned char *buf = __AFL_FUZZ_TESTCASE_BUF;
        while (__AFL_LOOP(10000)) {
            size_t len = __AFL_FUZZ_TESTCASE_LEN;
            if (len < 8) continue; /* minimal header */

            /* Build a memory-backed handle */
            typedef struct {
                const uint8_t *data;
                size_t size;
                size_t offset;
            } mem_buf_t;

            mem_buf_t mb = {
                .data = buf,
                .size = len,
                .offset = 0
            };

            /* Initialize client */
            vdi_stream_client_t *client = vdi_stream_client_init(&mb);
            if (!client) continue; /* skip invalid init */

            /* TODO: adapt for target function X */
            /* Example: read a chunk from the stream */
            uint8_t chunk[512];
            ssize_t n = vdi_stream_client_read(client, chunk, sizeof(chunk));
            (void)n; /* silence unused */

            /* Cleanup */
            vdi_stream_client_shutdown(client);
        }
        return 0;
    }
    ```

    Output ONLY valid JSON with:
    { "target_func": "vdi_stream_client_read", "input_format": "arbitrary byte stream consumed by vdi_stream_client via memory-backed thandle_t", "c_code": "...", "seed_commands": ["mkdir -p seeds/vdi_stream_client", "echo -n 'VDI0' > seeds/vdi_stream_client/seed0.bin", "echo -n 'VDI1' > seeds/vdi_stream_client/seed1.bin"], "compile_flags": "-g -O1 -fno-omit-frame-pointer" }
  api_func_fixes: {}
  magic_bytes: {}
recon_scoring:
  bonus_patterns: {}
  bonus_func_patterns:
    init: 12
    shutdown: 8
    read: 10
    seek: 9
    write: 5
    open: 8
    close: 7
    version: 3
  penalty_dirs:
  - test
  - tests
  - doc
  - docs
  - man
  penalty_files: []
  penalty_funcs: []
  low_value_files: {}
seeds:
  formats: {}
  oss_fuzz_corpus: $HOME/Nemesis/seeds/oss_fuzz_corpus_vdi_stream_client
  oss_fuzz_fuzzer_names: []
