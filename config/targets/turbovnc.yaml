# Auto-generated by `nemesis onboard`. Review all paths before running.
# Source scanned: /home/giorg/turbovnc_clean
# Build system:   cmake
# Language:       C
#
# External dependencies (CMake find_package):
#   Java               → (no apt hint; check distro packages)
#   X11                → (no apt hint; check distro packages)
#   Python             → (no apt hint; check distro packages)
#   JNI                → (no apt hint; check distro packages)
#   ZLIB               → apt install zlib1g-dev
#   OpenGL             → (no apt hint; check distro packages)
#
#   Bulk install: sudo apt install zlib1g-dev
#
# Oracle expansion candidates (Fix 148-150):
#   TSan candidate:  YES (threading tokens found in 7 file(s))
#                    - unix/Xvnc/programs/Xserver/os/utils.c
#                    - unix/Xvnc/programs/Xserver/os/inputthread.c
#                    - unix/Xvnc/programs/Xserver/include/globals.h
#                    + 4 more
#                    → enable: set sanitizer_profile: tsan,
#                      tsan_supported: true, AND threaded_oracle: true
#                      on at least one pinned_func.
#   MSan candidate:  MAYBE (external deps need MSan-rebuild: -lz)
#                    → before enabling, rebuild deps with -fsanitize=memory
#                      or accept a false-positive flood from those libs.
fuzzing:
  timeout_hours: 0.25
target:
  name: turbovnc
  oss_fuzz_project: turbovnc
  source_root: $HOME/turbovnc_clean
  work_root: $HOME/turbovnc_work
  build_dir: $HOME/turbovnc_work/build_fuzz
  debug_build_dir: $HOME/turbovnc_clean/build_debug
  ubsan_build_dir: $HOME/turbovnc_clean/build_ubsan
  coverage_build_dir: $HOME/turbovnc_clean/build_coverage
  build:
    configure: rm -f CMakeCache.txt && CC=afl-clang-fast CXX=afl-clang-fast++ cmake .. -DCMAKE_C_COMPILER=afl-clang-fast -DCMAKE_CXX_COMPILER=afl-clang-fast++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    make: make -j$(nproc) turbovnchelper
    debug_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    debug_make: make -j$(nproc) turbovnchelper
    ubsan_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    ubsan_make: make -j$(nproc) turbovnchelper
    coverage_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    coverage_make: make -j$(nproc) turbovnchelper
  repro_binary: ''
  repro_args: []
  source_subdir: unix/vncviewer
  include_subdir: unix/vncviewer
  library_name: unix/vncviewer/libturbovnchelper.a
  link_libs: -lz -lm
  harness_includes:
  - <Foundation/Foundation.h>
  harness_template: |+
    You are a fuzzing engineer generating AFL++ harnesses for TurboVNC/JRSwizzle.

    CRITICAL RULES:
    1. JRSwizzle is an Objective-C category on NSObject that swizzles methods at runtime. It does NOT parse or consume raw memory buffers. Fuzz input must be provided as Objective-C objects/messages, NOT as raw bytes.
    2. The harness must be written in Objective-C (not plain C) and must link against Foundation.framework. Use clang -framework Foundation for compilation.
    3. The library performs runtime method swizzling via class_addMethod/class_replaceMethod. Fuzzing this library requires generating valid Objective-C selectors and method signatures at runtime. Directly fuzzing raw memory is not applicable.
    4. There is NO public header that exposes a parsing/decoding API. The only public API is the JRSwizzle category on NSObject. Therefore, the harness must exercise swizzling by constructing Objective-C objects and invoking swizzle methods with fuzzed selectors and blocks.
    5. NEVER call private/internal selectors like jr_swizzleMethod:withBlock:error: directly from the harness. Use the public block-based API: jr_swizzleMethod:withBlock:error: and jr_swizzleClassMethod:withBlock:error:.
    6. Required #include: <Foundation/Foundation.h>
    7. AFL++ persistent mode is NOT applicable to Objective-C runtime swizzling harnesses. Use standard AFL++ mode (no __AFL_FUZZ_INIT, no __AFL_LOOP). The harness must accept a single file argument containing Objective-C source or serialized objects; the harness will parse and execute it via NSSelectorFromString/NSClassFromString.
    8. Forbidden patterns: allocating attacker-controlled sizes for method encoding, invoking swizzle with NULL blocks, or using invalid selectors that crash before swizzling occurs. These produce harness-induced false positives.
    9. The harness must NOT rely on any internal headers (e.g., no JRSwizzlePrivate.h). Only public Foundation.h is allowed.
    10. Seed files must be valid Objective-C snippets that define classes implementing methods to be swizzled (e.g., @interface MyClass : NSObject - (void)test; @end @implementation MyClass - (void)test { NSLog(@"test"); } @end). The harness will load these classes dynamically and swizzle their methods.

    FORBIDDEN INTERNAL FUNCTIONS:
    NEVER call these even if you see them used inside the target function source — they will fail to link.
    - jr_swizzleMethod:withMethod:error:
    - jr_swizzleClassMethod:withClassMethod:error:
    - class_addMethod
    - class_replaceMethod
    - method_exchangeImplementations
    - sel_getName
    - method_getName
    - imp_implementationWithBlock

    PROVEN WORKING TEMPLATE:
    ```c
    #import <Foundation/Foundation.h>

    int main(int argc, char **argv) {
        @autoreleasepool {
            if (argc < 2) {
                fprintf(stderr, "Usage: %s <objc_source_file>\n", argv[0]);
                return 1;
            }

            NSString *filePath = [NSString stringWithUTF8String:argv[1]];
            NSError *error = nil;
            NSString *objcSource = [NSString stringWithContentsOfFile:filePath encoding:NSUTF8StringEncoding error:&error];
            if (!objcSource) {
                fprintf(stderr, "Failed to read file: %s\n", [error.localizedDescription UTF8String]);
                return 1;
            }

            // Compile and load the Objective-C source dynamically
            NSTask *task = [[NSTask alloc] init];
            [task setLaunchPath:@"/usr/bin/osascript"];
            NSString *script = [NSString stringWithFormat:@"/usr/bin/env osascript -e 'do shell script \"echo %@ | /usr/bin/osascript -l JavaScript -e \"eval(ObjC.unwrap(ObjC.import(\'Foundation\'))\"'\"", objcSource];
            [task setArguments:@[@"-e", script]];
            [task launch];
            [task waitUntilExit];

            if ([task terminationStatus] != 0) {
                fprintf(stderr, "Objective-C compilation/execution failed\n");
                return 1;
            }

            // Attempt to swizzle a known method on a known class using fuzzed selector
            Class targetClass = NSClassFromString(@"NSObject");
            SEL targetSel = NSSelectorFromString(@"initWithCoder:");
            if (!targetSel) {
                fprintf(stderr, "Invalid selector generated\n");
                return 1;
            }

            // Use a fuzzed block (placeholder for fuzzer-generated block)
            id (^block)(id, NSCoder*) = ^id(id obj, NSCoder *coder) {
                NSLog(@"Swizzled initWithCoder: called with coder %@", coder);
                return obj;
            };

            // Perform swizzle
            NSError *swizzleError = nil;
            NSInvocation *invocation = [targetClass jr_swizzleMethod:targetSel withBlock:block error:&swizzleError];
            if (!invocation || swizzleError) {
                fprintf(stderr, "Swizzle failed: %s\n", [swizzleError.localizedDescription UTF8String]);
                return 1;
            }

            NSLog(@"Swizzle successful");
        }
        return 0;
    }
    ```

    Output ONLY valid JSON with:
    {
      "target_func": "function_name",
      "input_format": "turbovnc-specific input description",
      "c_code": "complete C harness source code using the template above",
      "seed_commands": ["shell commands to generate seed files"],
      "compile_flags": "-g -O1 -fno-omit-frame-pointer"
    }

  api_func_fixes: {}
  magic_bytes: {}
recon_scoring:
  bonus_patterns: {}
  bonus_func_patterns:
    swizzle: 15
    jr_: 12
    withBlock: 10
  penalty_dirs:
  - test
  - tests
  - doc
  - docs
  - man
  penalty_files: []
  penalty_funcs: []
  low_value_files: {}
seeds:
  formats: {}
  oss_fuzz_corpus: $HOME/Nemesis/seeds/oss_fuzz_corpus_turbovnc
  oss_fuzz_fuzzer_names: []
