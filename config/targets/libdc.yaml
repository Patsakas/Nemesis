# Auto-generated by `nemesis onboard`. Review all paths before running.
# Source scanned: /home/giorg/libdc_clean
# Build system:   autoconf
# Language:       C
#
# Oracle expansion candidates (Fix 148-150):
#   TSan candidate:  YES (threading tokens found in 1 file(s))
#                    - src/usbhid.c
#                    → enable: set sanitizer_profile: tsan,
#                      tsan_supported: true, AND threaded_oracle: true
#                      on at least one pinned_func.
#   MSan candidate:  YES (no external link deps that need MSan-rebuild)
#                    → enable: set sanitizer_profile: msan and
#                      msan_supported: true. Run as a SEPARATE campaign;
#                      mutually exclusive with the default ASAN build.
fuzzing:
  timeout_hours: 0.25
target:
  name: libdc
  oss_fuzz_project: libdc
  source_root: $HOME/libdc_clean
  work_root: $HOME/libdc_work
  build_dir: $HOME/libdc_work/build_fuzz
  debug_build_dir: $HOME/libdc_clean/build_debug
  ubsan_build_dir: $HOME/libdc_clean/build_ubsan
  coverage_build_dir: $HOME/libdc_clean/build_coverage
  build:
    configure: '{ (cd .. && make distclean) >/dev/null 2>&1; true; }; rm -f ../config.status ../config.log >/dev/null 2>&1 || true; [ -x ../configure ] || (cd .. && (./autogen.sh --help >/dev/null 2>&1 && ./autogen.sh --without-python || autoreconf -fi)) && CC=afl-clang-fast CXX=afl-clang-fast++ CFLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" CXXFLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" ../configure --enable-static --disable-shared --disable-examples --without-python'
    make: make -j$(nproc) -C src libdivecomputer.la
    debug_configure: '{ (cd .. && make distclean) >/dev/null 2>&1; true; }; rm -f ../config.status ../config.log >/dev/null 2>&1 || true; [ -x ../configure ] || (cd .. && (./autogen.sh --help >/dev/null 2>&1 && ./autogen.sh --without-python || autoreconf -fi)) && CC=clang CXX=clang++ CFLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" CXXFLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" ../configure --enable-static --disable-shared --disable-examples --without-python'
    debug_make: make -j$(nproc) -C src libdivecomputer.la
    ubsan_configure: '{ (cd .. && make distclean) >/dev/null 2>&1; true; }; rm -f ../config.status ../config.log >/dev/null 2>&1 || true; [ -x ../configure ] || (cd .. && (./autogen.sh --help >/dev/null 2>&1 && ./autogen.sh --without-python || autoreconf -fi)) && CC=clang CXX=clang++ CFLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" CXXFLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" ../configure --enable-static --disable-shared --disable-examples --without-python'
    ubsan_make: make -j$(nproc) -C src libdivecomputer.la
    coverage_configure: '{ (cd .. && make distclean) >/dev/null 2>&1; true; }; rm -f ../config.status ../config.log >/dev/null 2>&1 || true; [ -x ../configure ] || (cd .. && (./autogen.sh --help >/dev/null 2>&1 && ./autogen.sh --without-python || autoreconf -fi)) && CC=clang CXX=clang++ CFLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" CXXFLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" ../configure --enable-static --disable-shared --disable-examples --without-python'
    coverage_make: make -j$(nproc) -C src libdivecomputer.la
  repro_binary: ''
  repro_args: []
  source_subdir: src
  include_subdir: src
  library_name: src/.libs/libdivecomputer.a
  link_libs: -lm
  harness_includes:
  - aes.h
  harness_template: |+
    You are a fuzzing engineer generating AFL++ harnesses for libdc.

    CRITICAL RULES:
    1. Input is fed as a raw in-memory buffer via __AFL_FUZZ_TESTCASE_BUF/__AFL_FUZZ_TESTCASE_LEN; do NOT use FILE* or custom callbacks.
    2. The library operates on raw memory buffers; no handle/context object is required.
    3. No mandatory init/cleanup pairing beyond the standard main() harness.
    4. Forbidden patterns: allocating attacker-controlled sizes (e.g. malloc(input_len)) without internal bounds checks; avoid memcpy(dst, input, input_len) without validating dst bounds.
    5. Required #include list: <stdint.h>, "aes.h"
    6. AFL++ persistent mode is REQUIRED: use __AFL_FUZZ_INIT() at file scope, __AFL_LOOP(10000), __AFL_FUZZ_TESTCASE_BUF, __AFL_FUZZ_TESTCASE_LEN.
    7. Input length must be a multiple of 16 bytes for ECB/CBC modes; truncate or pad accordingly inside the loop.
    8. Never call internal helpers (none exposed in public headers) — the harness must only use public API.
    9. Avoid zero-length inputs; ensure at least 1 byte is present to trigger behavior.
    10. Do not mix encryption/decryption in the same harness iteration; pick one mode per harness variant.

    PROVEN WORKING TEMPLATE:
    ```c
    #include <stdint.h>
    #include "aes.h"

    __AFL_FUZZ_INIT();

    int main(int argc, char **argv) {
        __AFL_INIT();
        uint8_t key[16] = {0x2b, 0x7e, 0x15, 0x16, 0x28, 0xae, 0xd2, 0xa6,
                             0xab, 0xf7, 0x15, 0x88, 0x09, 0xcf, 0x4f, 0x3c};
        uint8_t iv[16] = {0x00, 0x01, 0x02, 0x03, 0x04, 0x05, 0x06, 0x07,
                            0x08, 0x09, 0x0a, 0x0b, 0x0c, 0x0d, 0x0e, 0x0f};

        while (__AFL_LOOP(10000)) {
            size_t len = __AFL_FUZZ_TESTCASE_LEN;
            if (len < 1) continue;
            /* Pad to multiple of 16 for CBC/ECB */
            size_t padded_len = ((len + 15) / 16) * 16;
            uint8_t *buf = __AFL_FUZZ_TESTCASE_BUF;
            uint8_t *out = malloc(padded_len);
            if (!out) continue;

            /* TODO: adapt for target function X */
            /* ECB encrypt */
            #if ECB
            AES128_ECB_encrypt(buf, key, out);
            #elif CBC
            AES128_CBC_encrypt_buffer(out, buf, len, key, iv);
            #else
            #error "No mode enabled in aes.h"
            #endif

            /* TODO: adapt for target function X */
            /* ECB decrypt */
            #if ECB
            AES128_ECB_decrypt(out, key, buf);
            #elif CBC
            AES128_CBC_decrypt_buffer(buf, out, len, key, iv);
            #endif

            free(out);
            (void)buf; /* silence unused */
        }
        return 0;
    }
    ```

    Output ONLY valid JSON with:
    {
      "target_func": "function_name",
      "input_format": "libdc-specific input description",
      "c_code": "complete C harness source code using the template above",
      "seed_commands": ["shell commands to generate seed files"],
      "compile_flags": "-g -O1 -fno-omit-frame-pointer"
    }

  api_func_fixes: {}
  magic_bytes: {}
recon_scoring:
  bonus_patterns: {}
  bonus_func_patterns:
    ECB_: 12
    CBC_: 12
    encrypt: 10
    decrypt: 10
  penalty_dirs:
  - test
  - tests
  - doc
  - docs
  - man
  penalty_files: []
  penalty_funcs: []
  low_value_files: {}
seeds:
  formats: {}
  oss_fuzz_corpus: $HOME/Nemesis/seeds/oss_fuzz_corpus_libdc
  oss_fuzz_fuzzer_names: []
