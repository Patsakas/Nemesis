# Auto-generated by `nemesis onboard`. Review all paths before running.
# Source scanned: /home/giorg/astera_clean
# Build system:   cmake
# Language:       C
#
# External dependencies (CMake find_package):
#   OpenAL             → (no apt hint; check distro packages)
#   OpenGL             → (no apt hint; check distro packages)
#   Doxygen            → (no apt hint; check distro packages)
#   X11                → (no apt hint; check distro packages)
#   ECM                → (no apt hint; check distro packages)
#   Wayland            → (no apt hint; check distro packages)
#   WaylandScanner     → (no apt hint; check distro packages)
#   WaylandProtocols   → (no apt hint; check distro packages)
#   XKBCommon          → (no apt hint; check distro packages)
#   EpollShim          → (no apt hint; check distro packages)
#   OSMesa             → (no apt hint; check distro packages)
#
# Oracle expansion candidates (Fix 148-150):
#   TSan candidate:  YES (threading tokens found in 2 file(s))
#                    - dep/glfw/src/posix_thread.h
#                    - dep/glfw/deps/tinycthread.h
#                    → enable: set sanitizer_profile: tsan,
#                      tsan_supported: true, AND threaded_oracle: true
#                      on at least one pinned_func.
#   MSan candidate:  YES (no external link deps that need MSan-rebuild)
#                    → enable: set sanitizer_profile: msan and
#                      msan_supported: true. Run as a SEPARATE campaign;
#                      mutually exclusive with the default ASAN build.
fuzzing:
  timeout_hours: 0.25
target:
  name: astera
  oss_fuzz_project: astera
  source_root: $HOME/astera_clean
  work_root: $HOME/astera_work
  build_dir: $HOME/astera_work/build_fuzz
  debug_build_dir: $HOME/astera_clean/build_debug
  ubsan_build_dir: $HOME/astera_clean/build_ubsan
  coverage_build_dir: $HOME/astera_clean/build_coverage
  build:
    configure: rm -f CMakeCache.txt && CC=afl-clang-fast CXX=afl-clang-fast++ cmake .. -DCMAKE_C_COMPILER=afl-clang-fast -DCMAKE_CXX_COMPILER=afl-clang-fast++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DGLFW_BUILD_EXAMPLES=OFF -DGLFW_BUILD_TESTS=OFF -DGLFW_BUILD_DOCS=OFF -DGLFW_VULKAN_STATIC=ON
    make: make -j$(nproc) glfw
    debug_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DGLFW_BUILD_EXAMPLES=OFF -DGLFW_BUILD_TESTS=OFF -DGLFW_BUILD_DOCS=OFF -DGLFW_VULKAN_STATIC=ON
    debug_make: make -j$(nproc) glfw
    ubsan_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DGLFW_BUILD_EXAMPLES=OFF -DGLFW_BUILD_TESTS=OFF -DGLFW_BUILD_DOCS=OFF -DGLFW_VULKAN_STATIC=ON
    ubsan_make: make -j$(nproc) glfw
    coverage_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DGLFW_BUILD_EXAMPLES=OFF -DGLFW_BUILD_TESTS=OFF -DGLFW_BUILD_DOCS=OFF -DGLFW_VULKAN_STATIC=ON
    coverage_make: make -j$(nproc) glfw
  repro_binary: ''
  repro_args: []
  source_subdir: dep/glfw/src
  include_subdir: include/astera
  library_name: dep/glfw/src/libglfw.a
  link_libs: -lm
  harness_includes:
  - asset.h
  harness_template: |+
    You are a fuzzing engineer generating AFL++ harnesses for astera.

    CRITICAL RULES:
    1. Feed the fuzzed data as an in‑memory buffer using `__AFL_FUZZ_TESTCASE_BUF` and `__AFL_FUZZ_TESTCASE_LEN`. The library can open a pak either from a file path or from memory; for fuzzing we will use the memory API `pak_open_mem`.
    2. The library requires a `pak_t*` handle returned by `pak_open_mem` (or `pak_open_file`). All subsequent calls must use this handle; never pass raw buffers directly to functions that expect a pak handle.
    3. Mandatory init/cleanup pairing: call `pak_open_mem` (or `pak_open_file`) to obtain a `pak_t*`, then after exercising the target API call `pak_close(pak)` to free resources. For asset maps, use `asset_map_create_mem` and later `asset_map_destroy`/`asset_map_free` as appropriate.
    4. Forbidden patterns: do NOT allocate a size based on attacker‑controlled values and then pass that size to internal helpers that lack bounds checks (e.g., avoid calling `pak_extract_noalloc` with a user‑controlled `out_cap` larger than the actual buffer). Also avoid calling any function that writes to the real filesystem (`asset_write`, `asset_write_data`, `pak_write_*`) because they can cause side‑effects and false positives.
    5. Required #include list: `#include <stdint.h>` (already pulled by the public header), `#include "asset.h"`, and AFL++ headers `#include <unistd.h>` for `size_t` if needed. No internal headers are needed.
    6. AFL++ persistent mode: place `__AFL_FUZZ_INIT();` at file scope immediately after includes. Wrap the main fuzz loop with `while (__AFL_LOOP(10000)) { ... }`. Use `__AFL_FUZZ_TESTCASE_BUF` and `__AFL_FUZZ_TESTCASE_LEN` inside the loop to build the memory‑backed pak.

    FORBIDDEN INTERNAL FUNCTIONS:
    NEVER call these even if you see them used inside the target function source — they will fail to link.
    - `pak_write_create()`
    - `pak_write_destroy()`
    - `pak_write_add_file()`
    - `pak_write_add_mem()`
    - `pak_write_to_file()`
    - `asset_write()`
    - `asset_write_data()`
    - `asset_map_free()` (use `asset_map_destroy` instead)
    - `asset_free()` (only call when you own the asset created via `asset_create`)
    - `pak_extract()` (allocates internally; use the no‑alloc variant for deterministic memory usage)

    PROVEN WORKING TEMPLATE:
    ```c
    #include <stdint.h>
    #include "asset.h"

    /* AFL++ persistent mode initialization – must be at file scope */
    __AFL_FUZZ_INIT();

    int main(int argc, char **argv) {
        /* AFL++ entry‑point init (separate from __AFL_FUZZ_INIT) */
        __AFL_INIT();

        while (__AFL_LOOP(10000)) {
            const uint8_t *data = __AFL_FUZZ_TESTCASE_BUF;
            size_t        len  = __AFL_FUZZ_TESTCASE_LEN;

            /* Guard against empty input – pak_open_mem expects non‑NULL data */
            if (len == 0) {
                continue;
            }

            /* Open the pak from the fuzzed memory buffer */
            pak_t *pak = pak_open_mem((unsigned char *)data, (uint32_t)len);
            if (!pak) {
                continue; /* malformed header – skip */
            }

            /* Example target API sequence: iterate over entries and extract without allocation */
            uint32_t count = pak_count(pak);
            for (uint32_t i = 0; i < count; ++i) {
                uint32_t size = pak_size(pak, i);
                /* Allocate a bounded buffer on the stack (max 4096) to avoid OOM */
                uint8_t out_buf[4096];
                uint32_t cap = (size < sizeof(out_buf)) ? size : sizeof(out_buf);
                uint32_t used = pak_extract_noalloc(pak, i, out_buf, cap);
                (void)used; /* suppress unused warning */
            }

            /* Clean up the pak handle */
            pak_close(pak);
        }
        return 0;
    }
    ```

    Output ONLY valid JSON with:
    {
      "target_func": "pak_open_mem",
      "input_format": "raw pak file bytes (memory‑backed)",
      "c_code": "<the complete C harness source code from the PROVEN WORKING TEMPLATE section>",
      "seed_commands": ["dd if=/dev/urandom of=seed.pak bs=1k count=1", "./pak_tool create seed.pak"],
      "compile_flags": "-g -O1 -fno-omit-frame-pointer"
    }

  api_func_fixes: {}
  magic_bytes: {}
recon_scoring:
  bonus_patterns: {}
  bonus_func_patterns:
    pak_open_mem: 12
    pak_extract_noalloc: 10
    pak_find: 8
    asset_map_create_mem: 11
  penalty_dirs:
  - test
  - tests
  - doc
  - docs
  - man
  penalty_files: []
  penalty_funcs: []
  low_value_files: {}
seeds:
  formats: {}
  oss_fuzz_corpus: $HOME/Nemesis/seeds/oss_fuzz_corpus_astera
  oss_fuzz_fuzzer_names: []
