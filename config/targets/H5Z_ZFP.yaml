# Auto-generated by `nemesis onboard`. Review all paths before running.
# Source scanned: /home/giorg/H5Z_ZFP_clean
# Build system:   cmake
# Language:       C
#
# External dependencies (CMake find_package):
#   MPI                → (no apt hint; check distro packages)
#   ZFP                → (no apt hint; check distro packages)
#   Sphinx             → (no apt hint; check distro packages)
#   HDF5               → (no apt hint; check distro packages)
#
# Oracle expansion candidates (Fix 148-150):
#   TSan candidate:  NO (no pthread/std::thread/OpenMP/_Atomic in source)
#   MSan candidate:  YES (no external link deps that need MSan-rebuild)
#                    → enable: set sanitizer_profile: msan and
#                      msan_supported: true. Run as a SEPARATE campaign;
#                      mutually exclusive with the default ASAN build.
fuzzing:
  timeout_hours: 0.25
target:
  name: H5Z_ZFP
  oss_fuzz_project: H5Z_ZFP
  source_root: $HOME/H5Z_ZFP_clean
  work_root: $HOME/H5Z_ZFP_work
  build_dir: $HOME/H5Z_ZFP_work/build_fuzz
  debug_build_dir: $HOME/H5Z_ZFP_clean/build_debug
  ubsan_build_dir: $HOME/H5Z_ZFP_clean/build_ubsan
  coverage_build_dir: $HOME/H5Z_ZFP_clean/build_coverage
  build:
    configure: rm -f CMakeCache.txt && CC=afl-clang-fast CXX=afl-clang-fast++ cmake .. -DCMAKE_C_COMPILER=afl-clang-fast -DCMAKE_CXX_COMPILER=afl-clang-fast++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DBUILD_TESTING=OFF
    make: make -j$(nproc) h5z_zfp_static
    debug_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DBUILD_TESTING=OFF
    debug_make: make -j$(nproc) h5z_zfp_static
    ubsan_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DBUILD_TESTING=OFF
    ubsan_make: make -j$(nproc) h5z_zfp_static
    coverage_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DBUILD_TESTING=OFF
    coverage_make: make -j$(nproc) h5z_zfp_static
  repro_binary: ''
  repro_args: []
  source_subdir: src
  include_subdir: src
  library_name: src/libh5z_zfp_static.a
  link_libs: -lm
  harness_includes:
  - H5Zzfp.h
  harness_template: |+
    You are a fuzzing engineer generating AFL++ harnesses for H5Z_ZFP.

    CRITICAL RULES:
    1. Feed the fuzz input as an in‑memory buffer using the AFL++ globals `__AFL_FUZZ_TESTCASE_BUF` and `__AFL_FUZZ_TESTCASE_LEN`.
    2. The H5Z_ZFP API operates on HDF5 dataset handles; you must create an HDF5 file object in memory and obtain a dataset handle before invoking compression/decompression functions.
    3. Always pair `H5Fclose()` with `H5Fopen()` and `H5Dclose()` with `H5Dcreate()`/`H5Dopen()`. Likewise, call `H5Z_zfp_register()` once before any compression calls and `H5Z_zfp_unregister()` at program exit.
    4. Do NOT allocate buffers whose size is derived directly from attacker‑controlled input without applying the library’s internal bounds checks (e.g. avoid `malloc(__AFL_FUZZ_TESTCASE_LEN)` for raw ZFP data unless the API validates the size).
    5. Required includes are listed in the "harness_includes" field below; include them in the exact order.
    6. AFL++ persistent mode must be used: place `__AFL_FUZZ_INIT();` at file scope right after the includes, use `__AFL_LOOP(10000)` in `main`, and reference `__AFL_FUZZ_TESTCASE_BUF`/`__AFL_FUZZ_TESTCASE_LEN` only inside the loop body.

    FORBIDDEN INTERNAL FUNCTIONS:
    NEVER call these even if you see them used inside the target function source — they will fail to link.
    - `ZFP_initialize()`
    - `ZFP_compress_block()`
    - `ZFP_decompress_block()`
    - `ZFP_set_stream()`
    - `ZFP_get_stream()`
    - `ZFP_free_context()`
    - `ZFP_alloc_buffer()`
    - `ZFP_release_buffer()`
    - `ZFP_internal_error()`
    - `ZFP_debug_print()`

    PROVEN WORKING TEMPLATE:
    ```c
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    #include "H5Zzfp.h"
    #include "hdf5.h"

    /* AFL++ persistent mode initialization */
    __AFL_FUZZ_INIT();

    /* Helper to create an in‑memory HDF5 file backed by a memory buffer */
    static hid_t create_mem_file(const uint8_t *data, size_t size) {
        hid_t fapl = H5Pcreate(H5P_FILE_ACCESS);
        if (fapl < 0) return -1;
        H5Pset_fapl_core(fapl, (size_t)1024, 0);
        hid_t file = H5Fopen("memfile.h5", H5F_ACC_RDWR, fapl);
        H5Pclose(fapl);
        return file;
    }

    int main(int argc, char **argv) {
        /* AFL++ entry point */
        __AFL_INIT();

        /* Register ZFP filter once */
        if (H5Z_zfp_register() < 0) {
            fprintf(stderr, "Failed to register ZFP filter\n");
            return 1;
        }

        while (__AFL_LOOP(10000)) {
            const uint8_t *buf = __AFL_FUZZ_TESTCASE_BUF;
            size_t len = __AFL_FUZZ_TESTCASE_LEN;

            /* Create an in‑memory HDF5 file */
            hid_t file = create_mem_file(buf, len);
            if (file < 0) continue; // skip if file creation failed

            /* Create a simple dataspace */
            hsize_t dims[2] = {4, 4};
            hid_t space = H5Screate_simple(2, dims, NULL);
            if (space < 0) { H5Fclose(file); continue; }

            /* Create a dataset with ZFP filter applied */
            hid_t dcpl = H5Pcreate(H5P_DATASET_CREATE);
            if (dcpl < 0) { H5Sclose(space); H5Fclose(file); continue; }
            /* Set ZFP filter properties – using defaults */
            if (H5Pset_filter(dcpl, H5Z_FILTER_ZFP, H5Z_FLAG_OPTIONAL, 0, NULL) < 0) {
                H5Pclose(dcpl); H5Sclose(space); H5Fclose(file); continue;
            }

            hid_t dset = H5Dcreate2(file, "dset", H5T_NATIVE_FLOAT, space,
                                     H5P_DEFAULT, dcpl, H5P_DEFAULT);
            if (dset < 0) {
                H5Pclose(dcpl); H5Sclose(space); H5Fclose(file); continue;
            }

            /* Write fuzz data into the dataset – the library will compress using ZFP */
            /* NOTE: The dataset expects float data; we simply write raw bytes; the filter will handle size checks */
            H5Dwrite(dset, H5T_NATIVE_FLOAT, H5S_ALL, H5S_ALL, H5P_DEFAULT, buf);

            /* Cleanup */
            H5Dclose(dset);
            H5Pclose(dcpl);
            H5Sclose(space);
            H5Fclose(file);
        }

        /* Unregister ZFP filter before exit */
        H5Z_zfp_unregister();
        return 0;
    }
    ```

    Output ONLY valid JSON with:
    {
      "target_func": "H5Dwrite",
      "input_format": "raw binary data fed to a ZFP‑compressed HDF5 dataset",
      "c_code": "<the full C harness source exactly as shown above>",
      "seed_commands": ["python3 -c 'import numpy as np, zfp; data=np.arange(16,dtype=np.float32); print(data.tobytes())' > seed.bin"],
      "compile_flags": "-g -O1 -fno-omit-frame-pointer"
    }

  api_func_fixes: {}
  magic_bytes: {}
recon_scoring:
  bonus_patterns: {}
  bonus_func_patterns:
    H5Z_zfp_register: 12
    H5Z_zfp_unregister: 12
    H5Dwrite: 10
    H5Dread: 10
  penalty_dirs:
  - test
  - tests
  - doc
  - docs
  - man
  penalty_files: []
  penalty_funcs: []
  low_value_files: {}
seeds:
  formats: {}
  oss_fuzz_corpus: $HOME/Nemesis/seeds/oss_fuzz_corpus_H5Z_ZFP
  oss_fuzz_fuzzer_names: []
