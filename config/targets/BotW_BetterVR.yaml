# Auto-generated by `nemesis onboard`. Review all paths before running.
# Source scanned: /home/giorg/BotW_BetterVR_clean
# Build system:   cmake
# Language:       C++
#
# External dependencies (CMake find_package):
#   OpenXR             → (no apt hint; check distro packages)
#   glm                → (no apt hint; check distro packages)
#   imgui              → (no apt hint; check distro packages)
#   implot             → (no apt hint; check distro packages)
#
# Oracle expansion candidates (Fix 148-150):
#   TSan candidate:  YES (threading tokens found in 7 file(s))
#                    - include/pch.h
#                    - src/utils/profiler.h
#                    - src/hooking/rumble.h
#                    + 4 more
#                    → enable: set sanitizer_profile: tsan,
#                      tsan_supported: true, AND threaded_oracle: true
#                      on at least one pinned_func.
#   MSan candidate:  MAYBE (external deps need MSan-rebuild: -lstdc++)
#                    → before enabling, rebuild deps with -fsanitize=memory
#                      or accept a false-positive flood from those libs.
fuzzing:
  timeout_hours: 0.25
target:
  name: BotW_BetterVR
  oss_fuzz_project: BotW_BetterVR
  source_root: $HOME/BotW_BetterVR_clean
  work_root: $HOME/BotW_BetterVR_work
  build_dir: $HOME/BotW_BetterVR_work/build_fuzz
  debug_build_dir: $HOME/BotW_BetterVR_clean/build_debug
  ubsan_build_dir: $HOME/BotW_BetterVR_clean/build_ubsan
  coverage_build_dir: $HOME/BotW_BetterVR_clean/build_coverage
  build:
    configure: rm -f CMakeCache.txt && CC=afl-clang-fast CXX=afl-clang-fast++ cmake .. -DCMAKE_C_COMPILER=afl-clang-fast -DCMAKE_CXX_COMPILER=afl-clang-fast++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DCMAKE_CXX_FLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    make: make -j$(nproc) BetterVR_Layer
    debug_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DCMAKE_CXX_FLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    debug_make: make -j$(nproc) BetterVR_Layer
    ubsan_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DCMAKE_CXX_FLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    ubsan_make: make -j$(nproc) BetterVR_Layer
    coverage_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DCMAKE_CXX_FLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    coverage_make: make -j$(nproc) BetterVR_Layer
  repro_binary: ''
  repro_args: []
  source_subdir: src
  include_subdir: include
  library_name: src/libbettervr_layer.a
  link_libs: -lpthread -lstdc++ -lm
  harness_includes:
  - cemu.h
  harness_template: |+
    You are a fuzzing engineer generating AFL++ harnesses for BotW_BetterVR.

    CRITICAL RULES:
    1. The library is a Wii U emulator helper layer; fuzzing targets the HLE (High-Level Emulation) function registration and PPCInterpreter_t state mutation via public API.
    2. Feed fuzz input as raw memory buffer via __AFL_FUZZ_TESTCASE_BUF/__AFL_FUZZ_TESTCASE_LEN; do NOT use FILE* or custom callbacks.
    3. The library exposes no handles/context objects for fuzzing; mutate PPCInterpreter_t state directly in memory.
    4. Mandatory init/cleanup: none required for harness; the emulator core is initialized externally. Only register/unregister functions are exposed.
    5. Forbidden patterns: never allocate memory with sizes controlled by fuzzer input without internal bounds checks (e.g., memcpy(dst, src, fuzz_len) unless the library provides a safe wrapper. Avoid direct memcpy into emulator memory regions.
    6. Required #include list: cemu.h
    7. AFL++ persistent mode: REQUIRED. Use __AFL_FUZZ_INIT() at file scope, __AFL_LOOP(10000), __AFL_FUZZ_TESTCASE_BUF, __AFL_FUZZ_TESTCASE_LEN.
    8. Never call internal emulator helpers (e.g., PPCInterpreter_run, memory_writeU32, osLib_loadedLibraries) — they are not in public headers and will fail to link.
    9. Target functions for fuzzing are registration helpers: osLib_registerHLEFunctionPtr_t and memory_getBasePtr_t; mutate their arguments via fuzzer input.
    10. Do NOT mutate VPADStatus or other runtime state structures directly; only mutate registration arguments and interpreter state via provided APIs.

    FORBIDDEN INTERNAL FUNCTIONS:
    - PPCInterpreter_run()
    - memory_writeU32()
    - memory_writeU64()
    - osLib_loadedLibraries()
    - gameMeta_getTitleId()
    - VPADRead()
    - HLEFunction()
    - memory_getVirtualOffset()
    - memory_getBasePtr()
    NEVER call these even if you see them used inside the target function source — they will fail to link.

    PROVEN WORKING TEMPLATE:
    ```c
    #include "cemu.h"
    __AFL_FUZZ_INIT();

    int main(int argc, char **argv)
    {
        __AFL_INIT();

        while (__AFL_LOOP(10000)) {
            size_t len = __AFL_FUZZ_TESTCASE_LEN;
            if (len < 16) continue; // need at least libraryName + functionName + hCPU pointer

            // Split input into libraryName (null-terminated), functionName (null-terminated), and hCPU pointer
            const char *libraryName = (const char *)__AFL_FUZZ_TESTCASE_BUF;
            size_t libLen = strnlen(libraryName, len);
            if (libLen == len) continue; // no null terminator
            const char *functionName = libraryName + libLen + 1;
            size_t funcLen = strnlen(functionName, len - libLen - 1);
            if (funcLen == len - libLen - 1) continue; // no null terminator
            if (libLen + funcLen + 2 > len) continue; // overflow

            // Ensure hCPU pointer is aligned and within reasonable range
            uintptr_t hcpuOff = (uintptr_t)(functionName + funcLen + 1);
            if (hcpuOff + sizeof(PPCInterpreter_t) > len) continue;
            PPCInterpreter_t *hCPU = (PPCInterpreter_t *)(functionName + funcLen + 1);

            // Register HLE function with fuzzer-controlled arguments
            osLib_registerHLEFunctionPtr_t regFunc = (osLib_registerHLEFunctionPtr_t)1; // dummy non-NULL to trigger registration path
            regFunc(libraryName, functionName, (void (*)(PPCInterpreter_t*))hCPU);

            // Optional: mutate interpreter state via memory_getBasePtr_t if needed
            memory_getBasePtr_t baseFunc = memory_getBasePtr;
            void *base = baseFunc();
            if (base && len > hcpuOff + sizeof(PPCInterpreter_t) + 8) {
                uint64_t *p = (uint64_t *)((uint8_t *)hCPU + sizeof(PPCInterpreter_t));
                *p = __AFL_FUZZ_TESTCASE_LEN; // harmless mutation to exercise interpreter state handling
            }

            /* TODO: adapt for target function X: replace osLib_registerHLEFunctionPtr_t call with other public API calls (e.g., memory_getBasePtr_t variants, gameMeta_getTitleIdPtr_t) */
        }
        return 0;
    }
    ```

    Output ONLY valid JSON with:
    {
      "target_func": "function_name",
      "input_format": "BotW_BetterVR-specific input description",
      "c_code": "complete C harness source code using the template above",
      "seed_commands": ["shell commands to generate seed files"],
      "compile_flags": "-g -O1 -fno-omit-frame-pointer"
    }

  api_func_fixes: {}
  magic_bytes: {}
recon_scoring:
  bonus_patterns: {}
  bonus_func_patterns:
    registerHLE: 15
    getBasePtr: 12
    getTitleId: 8
    VPAD: 10
    PPCInterpreter: 9
  penalty_dirs:
  - test
  - tests
  - doc
  - docs
  - man
  penalty_files: []
  penalty_funcs: []
  low_value_files: {}
seeds:
  formats: {}
  oss_fuzz_corpus: $HOME/Nemesis/seeds/oss_fuzz_corpus_BotW_BetterVR
  oss_fuzz_fuzzer_names: []
