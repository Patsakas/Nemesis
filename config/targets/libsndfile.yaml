# Auto-generated by `nemesis onboard`. Review all paths before running.
# Source scanned: $HOME/libsndfile_clean
# Build system:   autoconf
# Language:       C
#
# External dependencies (CMake find_package):
#   PkgConfig          → (no apt hint; check distro packages)
#
# Oracle expansion candidates (Fix 148-150):
#   TSan candidate:  NO (no pthread/std::thread/OpenMP/_Atomic in source)
#   MSan candidate:  YES (no external link deps that need MSan-rebuild)
#                    → enable: set sanitizer_profile: msan and
#                      msan_supported: true. Run as a SEPARATE campaign;
#                      mutually exclusive with the default ASAN build.
fuzzing:
  timeout_hours: 0.25
target:
  name: libsndfile
  oss_fuzz_project: libsndfile
  source_root: $HOME/libsndfile_clean
  work_root: $HOME/libsndfile_work
  build_dir: $HOME/libsndfile_work/build_fuzz
  debug_build_dir: $HOME/libsndfile_clean/build_debug
  ubsan_build_dir: $HOME/libsndfile_clean/build_ubsan
  coverage_build_dir: $HOME/libsndfile_clean/build_coverage
  build:
    configure: '[ -x ../configure ] || (cd .. && (./autogen.sh --help >/dev/null 2>&1 && ./autogen.sh --without-python || autoreconf -fi)) && CC=afl-clang-fast CXX=afl-clang-fast++ CFLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" CXXFLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" ../configure --enable-static --disable-shared --disable-external-libs --without-python'
    make: make -j$(nproc) -C src libsndfile.la
    debug_configure: '[ -x ../configure ] || (cd .. && (./autogen.sh --help >/dev/null 2>&1 && ./autogen.sh --without-python || autoreconf -fi)) && CC=clang CXX=clang++ CFLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" CXXFLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" ../configure --enable-static --disable-shared --disable-external-libs --without-python'
    debug_make: make -j$(nproc) -C src libsndfile.la
    ubsan_configure: '[ -x ../configure ] || (cd .. && (./autogen.sh --help >/dev/null 2>&1 && ./autogen.sh --without-python || autoreconf -fi)) && CC=clang CXX=clang++ CFLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" CXXFLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" ../configure --enable-static --disable-shared --disable-external-libs --without-python'
    ubsan_make: make -j$(nproc) -C src libsndfile.la
    coverage_configure: '[ -x ../configure ] || (cd .. && (./autogen.sh --help >/dev/null 2>&1 && ./autogen.sh --without-python || autoreconf -fi)) && CC=clang CXX=clang++ CFLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" CXXFLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" ../configure --enable-static --disable-shared --disable-external-libs --without-python'
    coverage_make: make -j$(nproc) -C src libsndfile.la
  repro_binary: ''
  repro_args: []
  source_subdir: src
  include_subdir: src
  library_name: src/.libs/libsndfile.a
  link_libs: -lm
  harness_includes:
  - sndfile.h
  harness_template: |
    You are a fuzzing engineer generating AFL++ harnesses for libsndfile.

    CRITICAL RULES:
    1. Feed fuzz input via memory-backed virtual I/O (SF_VIRTUAL_IO) using `sf_open_virtual()`. NEVER use FILE* or raw file paths.
    2. libsndfile requires a SNDFILE* handle obtained via `sf_open_virtual()` (or `sf_open()`/`sf_open_fd()`). All operations must use this handle.
    3. Mandatory init/cleanup: `sf_open_virtual()` → `sf_close()`. Every open must have a matching close.
    4. Forbidden: Allocating buffers with sizes derived from untrusted input without bounds checks. Use fixed-size stacks or AFL-managed buffers.
    5. Required includes: `<sndfile.h>`, `<stdint.h>`, `<stdlib.h>`, `<string.h>`.
    6. AFL++ persistent mode: Use `__AFL_FUZZ_INIT();` at file scope, then `__AFL_LOOP(10000)` in main(). Access input via `__AFL_FUZZ_TESTCASE_BUF` and `__AFL_FUZZ_TESTCASE_LEN`.
    7. For virtual I/O, define a `mem_buf_t` struct (data, size, offset) and implement callbacks (get_filelen, seek, read, write, tell) that operate on this struct. Pass its address as `user_data` to `sf_open_virtual()`.
    8. Always check `sf_error()` after `sf_open_virtual()` and abort if non-zero.
    9. Seed files should be valid WAV/FLAC/AIFF headers (even minimal ones) to avoid early rejects.
    10. NEVER call internal functions like `wav_open()`, `flac_init()`, or `aiff_read_header()` — they are not in public headers and will fail to link.

    FORBIDDEN INTERNAL FUNCTIONS:
    NEVER call these even if you see them used inside the target function source — they will fail to link: `wav_open()`, `wav_write_header()`, `flac_analyze_subframe_()`, `aiff_set_string()`, `ogg_write_page()`, `rf64_write_header()`, `pcm_read_block()`, `sndfile_ima_adpcm_init()`, `sndfile_gsm610_init()`, `sndfile_vorbis_init()`.

    PROVEN WORKING TEMPLATE:
    ```c
    #include <sndfile.h>
    #include <stdint.h>
    #include <stdlib.h>
    #include <string.h>

    __AFL_FUZZ_INIT();

    typedef struct {
        const uint8_t *data;
        size_t size;
        size_t offset;
    } mem_buf_t;

    static sf_count_t vio_get_filelen(void *user_data) {
        mem_buf_t *buf = (mem_buf_t *)user_data;
        return (sf_count_t)buf->size;
    }

    static sf_count_t vio_seek(sf_count_t offset, int whence, void *user_data) {
        mem_buf_t *buf = (mem_buf_t *)user_data;
        switch (whence) {
            case SEEK_SET: buf->offset = (size_t)offset; break;
            case SEEK_CUR: buf->offset += (size_t)offset; break;
            case SEEK_END: buf->offset = buf->size + (size_t)offset; break;
            default: return -1;
        }
        if (buf->offset > buf->size) buf->offset = buf->size;
        return (sf_count_t)buf->offset;
    }

    static sf_count_t vio_read(void *ptr, sf_count_t count, void *user_data) {
        mem_buf_t *buf = (mem_buf_t *)user_data;
        size_t to_read = (buf->offset + (size_t)count > buf->size) ? (buf->size - buf->offset) : (size_t)count;
        if (to_read == 0) return 0;
        memcpy(ptr, buf->data + buf->offset, to_read);
        buf->offset += to_read;
        return (sf_count_t)to_read;
    }

    static sf_count_t vio_write(const void *ptr, sf_count_t count, void *user_data) {
        (void)ptr; (void)count; (void)user_data;
        return 0; /* Write not supported for fuzzing */
    }

    static sf_count_t vio_tell(void *user_data) {
        mem_buf_t *buf = (mem_buf_t *)user_data;
        return (sf_count_t)buf->offset;
    }

    int main(int argc, char **argv) {
        __AFL_INIT();

        __AFL_LOOP(10000) {
            mem_buf_t mem_buf = {
                .data = __AFL_FUZZ_TESTCASE_BUF,
                .size = (size_t)__AFL_FUZZ_TESTCASE_LEN,
                .offset = 0
            };

            SF_VIRTUAL_IO sfv = {
                .get_filelen = vio_get_filelen,
                .seek = vio_seek,
                .read = vio_read,
                .write = vio_write,
                .tell = vio_tell
            };

            SF_INFO sfinfo = {0};
            SNDFILE *sndfile = sf_open_virtual(&sfv, SFM_READ, &sfinfo, &mem_buf);
            if (!sndfile || sf_error(sndfile) != SF_ERR_NO_ERROR) {
                sf_close(sndfile);
                continue; /* Skip invalid inputs */
            }

            /* TODO: adapt for target function X */
            /* Example: Read frames as float */
            float buf[1024];
            sf_count_t read_count = sf_readf_float(sndfile, buf, sizeof(buf) / sizeof(float) / sfinfo.channels);
            (void)read_count;

            sf_close(sndfile);
        }

        return 0;
    }
    ```

    Output ONLY valid JSON with:
    { "target_func": "function_name", "input_format": "libsndfile-specific input description", "c_code": "complete C harness source code using the template above", "seed_commands": ["shell commands to generate seed files"], "compile_flags": "-g -O1 -fno-omit-frame-pointer" }
  api_func_fixes: {}
  magic_bytes:
    WAV:
    - RIFF
    - 'WAVEfmt '
    AIFF:
    - FORM
    - AIFFCOMM
    FLAC:
    - fLaC
    OGG:
    - OggS
    CAF:
    - caff
    AU:
    - .snd
    RAW: []
recon_scoring:
  bonus_patterns: {}
  bonus_func_patterns:
    sf_open_: 15
    sf_read: 12
    sf_write: 12
    sf_command: 10
    sf_seek: 8
    sf_set_: 7
    sf_get_: 7
    parse_: 10
    decode_: 10
  penalty_dirs:
  - test
  - tests
  - doc
  - docs
  - man
  penalty_files: []
  penalty_funcs: []
  low_value_files: {}
seeds:
  formats: {}
  oss_fuzz_corpus: $HOME/Nemesis/seeds/oss_fuzz_corpus_libsndfile
  oss_fuzz_fuzzer_names: []
