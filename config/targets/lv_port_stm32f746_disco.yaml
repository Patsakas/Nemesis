# Auto-generated by `nemesis onboard`. Review all paths before running.
# Source scanned: /home/giorg/lv_port_stm32f746_disco_clean
# Build system:   cmake
# Language:       C
#
# Oracle expansion candidates (Fix 148-150):
#   TSan candidate:  NO (no pthread/std::thread/OpenMP/_Atomic in source)
#   MSan candidate:  YES (no external link deps that need MSan-rebuild)
#                    → enable: set sanitizer_profile: msan and
#                      msan_supported: true. Run as a SEPARATE campaign;
#                      mutually exclusive with the default ASAN build.
fuzzing:
  timeout_hours: 0.25
target:
  name: lv_port_stm32f746_disco
  oss_fuzz_project: lv_port_stm32f746_disco
  source_root: $HOME/lv_port_stm32f746_disco_clean
  work_root: $HOME/lv_port_stm32f746_disco_work
  build_dir: $HOME/lv_port_stm32f746_disco_work/build_fuzz
  debug_build_dir: $HOME/lv_port_stm32f746_disco_clean/build_debug
  ubsan_build_dir: $HOME/lv_port_stm32f746_disco_clean/build_ubsan
  coverage_build_dir: $HOME/lv_port_stm32f746_disco_clean/build_coverage
  build:
    configure: rm -f CMakeCache.txt && CC=afl-clang-fast CXX=afl-clang-fast++ cmake .. -DCMAKE_C_COMPILER=afl-clang-fast -DCMAKE_CXX_COMPILER=afl-clang-fast++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    make: make -j$(nproc) lv_port_stm32f746_disco
    debug_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    debug_make: make -j$(nproc) lv_port_stm32f746_disco
    ubsan_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    ubsan_make: make -j$(nproc) lv_port_stm32f746_disco
    coverage_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    coverage_make: make -j$(nproc) lv_port_stm32f746_disco
  repro_binary: ''
  repro_args: []
  source_subdir: src
  include_subdir: inc
  library_name: liblv_port_stm32f746_disco.a
  link_libs: -lm
  harness_includes:
  - stm32f7xx_it.h
  harness_template: |+
    You are a fuzzing engineer generating AFL++ harnesses for lv_port_stm32f746_disco.

    CRITICAL RULES:
    1. Feed the entire fuzz input as a raw memory buffer using __AFL_FUZZ_TESTCASE_BUF and __AFL_FUZZ_TESTCASE_LEN; the library does not read from files.
    2. The library functions operate directly on the provided buffer; no handle or context object is required for the exported interrupt handlers.
    3. No explicit init/cleanup is needed for the public API; however, ensure that any global state that might be touched by the handlers is reset between iterations by re‑entering the AFL persistent loop.
    4. NEVER allocate attacker‑controlled sizes for internal buffers without bounds checks – the interrupt handlers expect fixed‑size hardware registers and will crash on out‑of‑bounds writes.
    5. REQUIRED #include list: <stddef.h>, "stm32f7xx_it.h", "afl/types.h" (for AFL macros). Order matters: standard headers first, then the public header.
    6. AFL++ persistent mode must be used: place __AFL_FUZZ_INIT(); at file scope right after includes, then in main use __AFL_LOOP(10000) and the __AFL_FUZZ_TESTCASE_BUF / __AFL_FUZZ_TESTCASE_LEN macros inside the loop.
    7. Do NOT call any internal HAL/LL functions that are not declared in stm32f7xx_it.h; they will cause link errors.

    FORBIDDEN INTERNAL FUNCTIONS:
    NEVER call these even if you see them used inside the target function source — they will fail to link.
    - HAL_RCC_OscConfig()
    - HAL_RCC_ClockConfig()
    - HAL_PWR_EnableBkUpAccess()
    - __HAL_RCC_GPIOA_CLK_ENABLE()
    - __HAL_RCC_GPIOB_CLK_ENABLE()
    - HAL_GPIO_Init()
    - HAL_DMA_Init()
    - HAL_SD_MspInit()
    - HAL_SD_Init()
    - HAL_SD_ReadBlocks()

    PROVEN WORKING TEMPLATE:
    ```c
    #include <stddef.h>
    #include "stm32f7xx_it.h"

    /* AFL++ persistent mode initialization */
    __AFL_FUZZ_INIT();

    /* Optional: stub out any hardware‑specific functions that the handlers might call.
       For this generic harness we provide empty weak definitions to avoid link errors. */
    __attribute__((weak)) void NMI_Handler(void) {}
    __attribute__((weak)) void HardFault_Handler(void) {}
    __attribute__((weak)) void MemManage_Handler(void) {}
    __attribute__((weak)) void BusFault_Handler(void) {}
    __attribute__((weak)) void UsageFault_Handler(void) {}
    __attribute__((weak)) void SVC_Handler(void) {}
    __attribute__((weak)) void DebugMon_Handler(void) {}
    __attribute__((weak)) void PendSV_Handler(void) {}
    __attribute__((weak)) void SysTick_Handler(void) {}
    __attribute__((weak)) void BSP_SDMMC_IRQHandler(void) {}
    __attribute__((weak)) void BSP_SDMMC_DMA_Rx_IRQHandler(void) {}

    int main(int argc, char **argv) {
        /* AFL++ entry point */
        __AFL_INIT();

        while (__AFL_LOOP(10000)) {
            const uint8_t *data = __AFL_FUZZ_TESTCASE_BUF;
            size_t size = __AFL_FUZZ_TESTCASE_LEN;

            /* The interrupt handlers do not consume the buffer, but we can simulate
               a scenario where the fuzz data influences a global flag or a mock register.
               For demonstration we simply ignore the data and invoke each exported
               handler once. */
            (void)data; (void)size; // suppress unused warnings

            /* TODO: adapt for target function X */
            NMI_Handler();
            HardFault_Handler();
            MemManage_Handler();
            BusFault_Handler();
            UsageFault_Handler();
            SVC_Handler();
            DebugMon_Handler();
            PendSV_Handler();
            SysTick_Handler();
            BSP_SDMMC_IRQHandler();
            BSP_SDMMC_DMA_Rx_IRQHandler();
        }
        return 0;
    }
    ```

    Output ONLY valid JSON with:
    {
      "target_func": "NMI_Handler",
      "input_format": "raw binary buffer fed to AFL++, no specific file format",
      "c_code": "<the complete C harness source code from the PROVEN WORKING TEMPLATE section>",
      "seed_commands": ["printf '\x00' > seed0.bin", "printf '\xFF' > seed1.bin"],
      "compile_flags": "-g -O1 -fno-omit-frame-pointer"
    }

  api_func_fixes: {}
  magic_bytes: {}
recon_scoring:
  bonus_patterns: {}
  bonus_func_patterns: {}
  penalty_dirs:
  - test
  - tests
  - doc
  - docs
  - man
  penalty_files: []
  penalty_funcs: []
  low_value_files: {}
seeds:
  formats: {}
  oss_fuzz_corpus: $HOME/Nemesis/seeds/oss_fuzz_corpus_lv_port_stm32f746_disco
  oss_fuzz_fuzzer_names: []
