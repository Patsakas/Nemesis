# Auto-generated by `nemesis onboard`. Review all paths before running.
# Source scanned: $HOME/libcue_clean
# Build system:   cmake
# Language:       C
#
# Oracle expansion candidates (Fix 148-150):
#   TSan candidate:  NO (no pthread/std::thread/OpenMP/_Atomic in source)
#   MSan candidate:  YES (no external link deps that need MSan-rebuild)
#                    → enable: set sanitizer_profile: msan and
#                      msan_supported: true. Run as a SEPARATE campaign;
#                      mutually exclusive with the default ASAN build.
fuzzing:
  timeout_hours: 0.25
target:
  name: libcue
  oss_fuzz_project: libcue
  source_root: $HOME/libcue_clean
  work_root: $HOME/libcue_work
  build_dir: $HOME/libcue_work/build_fuzz
  debug_build_dir: $HOME/libcue_clean/build_debug
  ubsan_build_dir: $HOME/libcue_clean/build_ubsan
  coverage_build_dir: $HOME/libcue_clean/build_coverage
  build:
    configure: rm -f CMakeCache.txt && CC=afl-clang-fast CXX=afl-clang-fast++ cmake .. -DCMAKE_C_COMPILER=afl-clang-fast -DCMAKE_CXX_COMPILER=afl-clang-fast++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    make: make -j$(nproc) cue
    debug_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    debug_make: make -j$(nproc) cue
    ubsan_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    ubsan_make: make -j$(nproc) cue
    coverage_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    coverage_make: make -j$(nproc) cue
  repro_binary: ''
  repro_args: []
  source_subdir: ''
  include_subdir: ''
  library_name: libcue.a
  link_libs: -lm
  harness_includes:
  - libcue.h
  harness_template: |
    You are a fuzzing engineer generating AFL++ harnesses for libcue.

    CRITICAL RULES:
    1. Feed fuzz input as a NULL-terminated string via `cue_parse_string()` or as a FILE* via `cue_parse_file()`. For fuzzing, use `cue_parse_string()` with input from `__AFL_FUZZ_TESTCASE_BUF` (ensure NULL-termination by allocating +1 byte and setting the last byte to 0).
    2. The library operates on opaque `Cd*` handles. Always pair `cue_parse_string()`/`cue_parse_file()` with `cd_delete()` to avoid memory leaks.
    3. Never pass attacker-controlled sizes directly to `malloc`/`calloc` without bounds checks. The input buffer is already sized by `__AFL_FUZZ_TESTCASE_LEN`; use that for allocations.
    4. Required includes: `<stdio.h>`, `<stdlib.h>`, `<string.h>`, and `<libcue.h>`.
    5. AFL++ persistent mode: Use `__AFL_FUZZ_INIT();` at file scope, then `__AFL_LOOP(10000)` in `main()`. Access input via `__AFL_FUZZ_TESTCASE_BUF` (uint8_t*) and `__AFL_FUZZ_TESTCASE_LEN` (size_t).
    6. The library does not use custom I/O callbacks (no `thandle_t` pattern needed).
    7. Forbidden: Do not use `FILE*` with fuzz input unless you create a temporary file (inefficient for fuzzing). Prefer `cue_parse_string()`.
    8. Always NULL-terminate the input buffer for `cue_parse_string()`.

    FORBIDDEN INTERNAL FUNCTIONS:
    NEVER call these even if you see them used inside the target function source — they will fail to link.
    `cd_new()`, `track_new()`, `cdtext_new()`, `rem_new()`, `cd_add_track()`, `track_set_filename()`, `track_set_mode()`, `cdtext_set()`, `rem_set()`, `cd_verify()`.

    PROVEN WORKING TEMPLATE:
    ```c
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    #include <libcue.h>

    __AFL_FUZZ_INIT();

    int main(int argc, char **argv) {
        __AFL_INIT();

        __AFL_LOOP(10000) {
            /* Allocate NULL-terminated buffer for cue_parse_string */
            char *input = malloc(__AFL_FUZZ_TESTCASE_LEN + 1);
            if (!input) return 1;
            memcpy(input, __AFL_FUZZ_TESTCASE_BUF, __AFL_FUZZ_TESTCASE_LEN);
            input[__AFL_FUZZ_TESTCASE_LEN] = '\0';

            /* Parse the CUE sheet */
            Cd *cd = cue_parse_string(input);
            free(input);

            if (cd) {
                /* Example: iterate tracks to trigger deeper parsing */
                int ntrack = cd_get_ntrack(cd);
                for (int i = 0; i < ntrack; i++) {
                    Track *track = cd_get_track(cd, i);
                    if (track) {
                        const char *filename = track_get_filename(track);
                        long start = track_get_start(track);
                        enum TrackMode mode = track_get_mode(track);
                        /* TODO: adapt for target function X (e.g., cd_get_cdtext, track_get_rem) */
                    }
                }
                cd_delete(cd);
            }
        }
        return 0;
    }
    ```

    Output ONLY valid JSON with:
    { "target_func": "function_name", "input_format": "libcue-specific input description", "c_code": "complete C harness source code using the template above", "seed_commands": ["shell commands to generate seed files"], "compile_flags": "-g -O1 -fno-omit-frame-pointer" }
  api_func_fixes: {}
  magic_bytes:
    CUE:
    - FILE "
    - 'CATALOG '
    - 'CDTEXTFILE '
    - 'TRACK '
    - 'INDEX '
    - 'REM '
    - 'FLAGS '
    CUE_BINARY: []
recon_scoring:
  bonus_patterns: {}
  bonus_func_patterns:
    parse_: 15
    cue_parse: 15
    cd_get_: 10
    track_get_: 10
    cdtext_get: 8
    rem_get: 8
  penalty_dirs:
  - test
  - tests
  - doc
  - docs
  - man
  penalty_files: []
  penalty_funcs: []
  low_value_files: {}
seeds:
  formats: {}
  oss_fuzz_corpus: $HOME/Nemesis/seeds/oss_fuzz_corpus_libcue
  oss_fuzz_fuzzer_names: []
