# Auto-generated by `nemesis onboard`. Review all paths before running.
# Source scanned: /home/giorg/onomondo_uicc_clean
# Build system:   cmake
# Language:       C
#
# Oracle expansion candidates (Fix 148-150):
#   TSan candidate:  NO (no pthread/std::thread/OpenMP/_Atomic in source)
#   MSan candidate:  YES (no external link deps that need MSan-rebuild)
#                    → enable: set sanitizer_profile: msan and
#                      msan_supported: true. Run as a SEPARATE campaign;
#                      mutually exclusive with the default ASAN build.
fuzzing:
  timeout_hours: 0.25
target:
  name: onomondo_uicc
  oss_fuzz_project: onomondo_uicc
  source_root: $HOME/onomondo_uicc_clean
  work_root: $HOME/onomondo_uicc_work
  build_dir: $HOME/onomondo_uicc_work/build_fuzz
  debug_build_dir: $HOME/onomondo_uicc_clean/build_debug
  ubsan_build_dir: $HOME/onomondo_uicc_clean/build_ubsan
  coverage_build_dir: $HOME/onomondo_uicc_clean/build_coverage
  build:
    configure: rm -f CMakeCache.txt && CC=afl-clang-fast CXX=afl-clang-fast++ cmake .. -DCMAKE_C_COMPILER=afl-clang-fast -DCMAKE_CXX_COMPILER=afl-clang-fast++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    make: make -j$(nproc) uicc
    debug_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    debug_make: make -j$(nproc) uicc
    ubsan_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    ubsan_make: make -j$(nproc) uicc
    coverage_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    coverage_make: make -j$(nproc) uicc
  repro_binary: ''
  repro_args: []
  source_subdir: src/softsim/uicc
  include_subdir: src/softsim/uicc
  library_name: src/softsim/uicc/libuicc.a
  link_libs: -lm
  harness_includes:
  - access.h
  harness_template: |
    You are a fuzzing engineer generating AFL++ harnesses for onomondo_uicc.

    CRITICAL RULES:
    1. Feed the fuzz input as a raw in‑memory buffer using the AFL++ globals `__AFL_FUZZ_TESTCASE_BUF` and `__AFL_FUZZ_TESTCASE_LEN`. The library expects APDU structures, so the harness must copy the raw bytes into a `struct ss_apdu` before calling any API.
    2. The library operates on opaque objects (`struct ss_lchan` and `struct ss_apdu`). You must allocate and initialise a `struct ss_lchan` with `ss_access_populate()` before invoking `ss_access_check_command()`.
    3. Mandatory init/cleanup pairing: allocate `struct ss_lchan` (e.g. on the stack or via malloc), call `ss_access_populate()` once per iteration, and do not free any internal resources (the library does not expose a shutdown function). If you allocate with malloc, free it after the loop iteration.
    4. Forbidden patterns: never allocate buffers whose size is derived directly from attacker‑controlled fuzz data without imposing an upper bound, as the library does not perform internal length checks on the APDU payload. Clamp any length fields to a safe maximum (e.g. 256 bytes) before populating the APDU.
    5. Required #include list: `#include <stddef.h>`, `#include <stdbool.h>`, `#include "access.h"`, and the AFL++ headers `#include <unistd.h>` (for the AFL macros) if needed.
    6. AFL++ persistent mode requirements: place `__AFL_FUZZ_INIT();` at file scope immediately after the includes, use `__AFL_LOOP(10000)` in `main`, and reference `__AFL_FUZZ_TESTCASE_BUF` / `__AFL_FUZZ_TESTCASE_LEN` inside the loop body.

    PROVEN WORKING TEMPLATE:
    ```c
    #include <stddef.h>
    #include <stdbool.h>
    #include "access.h"

    /* AFL++ persistent mode initialization – must be at file scope */
    __AFL_FUZZ_INIT();

    /* Helper to safely copy fuzz data into an APDU structure */
    static void build_apdu_from_input(const uint8_t *data, size_t len, struct ss_apdu *apdu) {
        /* Zero‑initialize the APDU */
        memset(apdu, 0, sizeof(*apdu));

        /* The APDU format is library‑specific; for fuzzing we treat the input as raw payload */
        size_t copy_len = len < sizeof(apdu->data) ? len : sizeof(apdu->data);
        memcpy(apdu->data, data, copy_len);
        apdu->len = (uint16_t)copy_len;  /* clamp to 16‑bit length field */
    }

    int main(int argc, char **argv) {
        /* AFL++ entry‑point macro */
        __AFL_INIT();

        while (__AFL_LOOP(10000)) {
            const uint8_t *buf = __AFL_FUZZ_TESTCASE_BUF;
            size_t      sz  = __AFL_FUZZ_TESTCASE_LEN;

            /* Prepare a channel object */
            struct ss_lchan lchan;
            ss_access_populate(&lchan);

            /* Build an APDU from the fuzz input */
            struct ss_apdu apdu;
            build_apdu_from_input(buf, sz, &apdu);

            /* Exercise a representative target function */
            /* TODO: adapt for target function ss_access_check_command */
            (void)ss_access_check_command(&apdu, SS_ACCESS_INTENTION_EF_READ);
        }
        return 0;
    }
    ```

    Output ONLY valid JSON with:
    {
      "target_func": "ss_access_check_command",
      "input_format": "raw APDU byte buffer (clamped to APDU data field size)",
      "c_code": "<the C harness above>",
      "seed_commands": ["dd if=/dev/urandom of=seed.apdu bs=1 count=64"],
      "compile_flags": "-g -O1 -fno-omit-frame-pointer"
    }
  api_func_fixes: {}
  magic_bytes: {}
recon_scoring:
  bonus_patterns: {}
  bonus_func_patterns:
    check_command: 12
    populate: 8
  penalty_dirs:
  - test
  - tests
  - doc
  - docs
  - man
  penalty_files: []
  penalty_funcs: []
  low_value_files: {}
seeds:
  formats: {}
  oss_fuzz_corpus: $HOME/Nemesis/seeds/oss_fuzz_corpus_onomondo_uicc
  oss_fuzz_fuzzer_names: []
