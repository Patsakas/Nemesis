# Auto-generated by `nemesis onboard`. Review all paths before running.
# Source scanned: /home/giorg/nmea_probe/libnmea
# Build system:   cmake
# Language:       C
#
# Oracle expansion candidates (Fix 148-150):
#   TSan candidate:  NO (no pthread/std::thread/OpenMP/_Atomic in source)
#   MSan candidate:  YES (no external link deps that need MSan-rebuild)
#                    → enable: set sanitizer_profile: msan and
#                      msan_supported: true. Run as a SEPARATE campaign;
#                      mutually exclusive with the default ASAN build.
fuzzing:
  timeout_hours: 0.25
target:
  name: libnmea
  oss_fuzz_project: libnmea
  source_root: $HOME/libnmea_clean
  work_root: /home/giorg/nmea_probe/libnmea_work
  build_dir: /home/giorg/nmea_probe/libnmea_work/build_fuzz
  debug_build_dir: $HOME/libnmea_clean/build_debug
  ubsan_build_dir: $HOME/libnmea_clean/build_ubsan
  coverage_build_dir: $HOME/libnmea_clean/build_coverage
  build:
    configure: rm -f CMakeCache.txt && CC=afl-clang-fast CXX=afl-clang-fast++ cmake .. -DCMAKE_C_COMPILER=afl-clang-fast -DCMAKE_CXX_COMPILER=afl-clang-fast++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DNMEA_BUILD_EXAMPLES=OFF -DNMEA_EXAMPLES_LINK_STATIC=ON -DNMEA_UNIT_TESTS=OFF -DNMEA_UNIT_TESTS_LINK_STATIC=ON
    make: make -j$(nproc) nmea
    debug_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DNMEA_BUILD_EXAMPLES=OFF -DNMEA_EXAMPLES_LINK_STATIC=ON -DNMEA_UNIT_TESTS=OFF -DNMEA_UNIT_TESTS_LINK_STATIC=ON
    debug_make: make -j$(nproc) nmea
    ubsan_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DNMEA_BUILD_EXAMPLES=OFF -DNMEA_EXAMPLES_LINK_STATIC=ON -DNMEA_UNIT_TESTS=OFF -DNMEA_UNIT_TESTS_LINK_STATIC=ON
    ubsan_make: make -j$(nproc) nmea
    coverage_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DNMEA_BUILD_EXAMPLES=OFF -DNMEA_EXAMPLES_LINK_STATIC=ON -DNMEA_UNIT_TESTS=OFF -DNMEA_UNIT_TESTS_LINK_STATIC=ON
    coverage_make: make -j$(nproc) nmea
  repro_binary: ''
  repro_args: []
  source_subdir: ''
  include_subdir: src/nmea
  library_name: libnmea.a
  link_libs: -lm
  harness_includes:
  - nmea.h
  harness_template: |
    You are a fuzzing engineer generating AFL++ harnesses for libnmea.

    CRITICAL RULES:
    1. Feed the entire fuzz input as an in‑memory buffer representing a raw NMEA sentence. Use `__AFL_FUZZ_TESTCASE_BUF` and `__AFL_FUZZ_TESTCASE_LEN` to obtain the data and length.
    2. The library operates on raw `char *` strings; no handle or context object is required. Pass the buffer directly to the public API functions.
    3. No explicit init/cleanup is needed for the library itself, but any `nmea_s *` returned by `nmea_parse` must be freed with `nmea_free` to avoid memory leaks.
    4. Never allocate a buffer of attacker‑controlled size and then pass it to the library without first ensuring it does not exceed `NMEA_MAX_LENGTH`. Enforce `len <= NMEA_MAX_LENGTH` before calling any API.
    5. Required includes are exactly: `#include <stddef.h>` and `#include "nmea.h"`.
    6. Use AFL++ persistent mode correctly: place `__AFL_FUZZ_INIT();` at file scope immediately after the includes, then in `main` call `__AFL_INIT();` and wrap the test loop with `while (__AFL_LOOP(10000)) { ... }`. Access the test case via `__AFL_FUZZ_TESTCASE_BUF` and `__AFL_FUZZ_TESTCASE_LEN` inside the loop.
    7. Do not call any internal helper functions that are not declared in `nmea.h`. The library has no separate private headers, so this rule is a safeguard against future internal additions.
    8. Always ensure the input ends with the required `\r\n` sequence before invoking `nmea_validate`; otherwise the function will return an error and the harness will continue safely.
    9. After parsing, if a non‑NULL `nmea_s *` is returned, immediately free it with `nmea_free` before the next iteration.
    10. Avoid printing or logging inside the harness; keep the code minimal to let AFL++ measure coverage accurately.

    PROVEN WORKING TEMPLATE:
    ```c
    #include <stddef.h>
    #include "nmea.h"

    /* AFL++ persistent mode initialization – must be at file scope */
    __AFL_FUZZ_INIT();

    static int is_valid_nmea(const uint8_t *data, size_t len) {
        /* Ensure we have room for the mandatory \r\n terminators */
        if (len < 2) return 0;
        if (data[len-2] != (uint8_t)NMEA_END_CHAR_1 || data[len-1] != (uint8_t)NMEA_END_CHAR_2) return 0;
        return 1;
    }

    int main(int argc, char **argv) {
        __AFL_INIT();
        while (__AFL_LOOP(10000)) {
            const uint8_t *buf = __AFL_FUZZ_TESTCASE_BUF;
            size_t          len = __AFL_FUZZ_TESTCASE_LEN;

            /* Enforce maximum NMEA sentence length to avoid out‑of‑bounds reads */
            if (len > NMEA_MAX_LENGTH) continue;
            if (!is_valid_nmea(buf, len)) continue;

            /* The API expects a mutable C string, so copy into a temporary buffer and NUL‑terminate */
            char tmp[NMEA_MAX_LENGTH + 1];
            memcpy(tmp, buf, len);
            tmp[len] = '\0';

            /* 1. Identify sentence type */
            nmea_t type = nmea_get_type(tmp);
            (void)type; /* suppress unused warning */

            /* 2. Compute checksum (optional, just for coverage) */
            uint8_t cs = nmea_get_checksum(tmp);
            (void)cs;

            /* 3. Validate the sentence */
            int valid = nmea_validate(tmp, len, 1);
            (void)valid;

            /* 4. Parse the sentence into a struct */
            nmea_s *parsed = nmea_parse(tmp, len, 1);
            if (parsed) {
                /* Use a few fields to increase coverage */
                int err = parsed->errors;
                nmea_t ptype = parsed->type;
                (void)err; (void)ptype;
                nmea_free(parsed);
            }
            /* TODO: adapt for target function X */
        }
        return 0;
    }
    ```

    Output ONLY valid JSON with:
    {
      "target_func": "nmea_parse",
      "input_format": "raw NMEA sentence ending with \r\n, max length 82 bytes",
      "c_code": "<the C harness source exactly as shown above>",
      "seed_commands": ["printf '$GPGGA,123519,4807.038,N,01131.000,E,1,08,0.9,545.4,M,46.9,M,,*47\r\n' > seed1.nmea", "printf '$GPRMC,235947,A,5540.123,N,03736.123,E,0.0,0.0,060180,0.0,W*6A\r\n' > seed2.nmea"],
      "compile_flags": "-g -O1 -fno-omit-frame-pointer"
    }
  api_func_fixes: {}
  magic_bytes: {}
recon_scoring:
  bonus_patterns: {}
  bonus_func_patterns:
    parse: 12
    validate: 10
    checksum: 8
  penalty_dirs:
  - test
  - tests
  - doc
  - docs
  - man
  penalty_files: []
  penalty_funcs: []
  low_value_files: {}
seeds:
  formats: {}
  oss_fuzz_corpus: $HOME/Nemesis/seeds/oss_fuzz_corpus_libnmea
  oss_fuzz_fuzzer_names: []
