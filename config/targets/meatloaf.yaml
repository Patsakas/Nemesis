# Auto-generated by `nemesis onboard`. Review all paths before running.
# Source scanned: /home/giorg/meatloaf_clean
# Build system:   cmake
# Language:       C++
#
# External dependencies (CMake find_package):
#   GSSAPI             → (no apt hint; check distro packages)
#   OpenSSL            → apt install libssl-dev
#
#   Bulk install: sudo apt install libssl-dev
#
# Oracle expansion candidates (Fix 148-150):
#   TSan candidate:  YES (threading tokens found in 20 file(s))
#                    - lib/hardware/fnSystem.h
#                    - lib/meatloaf/network/afp.h
#                    - components/libssh/config.h
#                    + 17 more
#                    → enable: set sanitizer_profile: tsan,
#                      tsan_supported: true, AND threaded_oracle: true
#                      on at least one pinned_func.
#   MSan candidate:  MAYBE (external deps need MSan-rebuild: -lstdc++ -lcrypto -lssl)
#                    → before enabling, rebuild deps with -fsanitize=memory
#                      or accept a false-positive flood from those libs.
fuzzing:
  timeout_hours: 0.25
target:
  name: meatloaf
  oss_fuzz_project: meatloaf
  source_root: $HOME/meatloaf_clean
  work_root: $HOME/meatloaf_work
  build_dir: $HOME/meatloaf_work/build_fuzz
  debug_build_dir: $HOME/meatloaf_clean/build_debug
  ubsan_build_dir: $HOME/meatloaf_clean/build_ubsan
  coverage_build_dir: $HOME/meatloaf_clean/build_coverage
  build:
    configure: rm -f CMakeCache.txt && CC=afl-clang-fast CXX=afl-clang-fast++ cmake .. -DCMAKE_C_COMPILER=afl-clang-fast -DCMAKE_CXX_COMPILER=afl-clang-fast++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DCMAKE_CXX_FLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DENABLE_EXAMPLES=OFF
    make: make -j$(nproc) libsmb2
    debug_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DCMAKE_CXX_FLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DENABLE_EXAMPLES=OFF
    debug_make: make -j$(nproc) libsmb2
    ubsan_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DCMAKE_CXX_FLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DENABLE_EXAMPLES=OFF
    ubsan_make: make -j$(nproc) libsmb2
    coverage_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DCMAKE_CXX_FLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DENABLE_EXAMPLES=OFF
    coverage_make: make -j$(nproc) libsmb2
  repro_binary: ''
  repro_args: []
  source_subdir: components/libsmb2
  include_subdir: include
  library_name: components/libsmb2/liblibsmb2.a
  link_libs: -lpthread -lstdc++ -lcrypto -lssl -lm
  harness_includes:
  - PSRAMAllocator.h
  harness_template: |
    You are a fuzzing engineer generating AFL++ harnesses for meatloaf.

    CRITICAL RULES:
    1. Feed fuzz input as a raw in‑memory buffer using __AFL_FUZZ_TESTCASE_BUF and __AFL_FUZZ_TESTCASE_LEN. Do NOT treat the input as a file or stream.
    2. The library operates purely on in‑memory objects; no handle/context object is required beyond the allocator instance you create.
    3. Always pair allocation with deallocation: allocate → use → deallocate. If you allocate with PSRAMAllocator<T>::allocate, you must free the pointer with PSRAMAllocator<T>::deallocate (or via PSRAMDeleter<T> in a smart‑pointer).
    4. NEVER allocate a size derived directly from attacker‑controlled input without an explicit upper bound check. Apply a sane cap (e.g. 1<<20 bytes) before calling allocate() to avoid false positives and OOM crashes.
    5. Required #include list: <stddef.h>, <stdint.h>, <stdio.h>, <string.h>, "PSRAMAllocator.h".
    6. AFL++ persistent mode must be used: place __AFL_FUZZ_INIT(); at file scope right after the includes, then use __AFL_LOOP(10000) inside main, and access the testcase via __AFL_FUZZ_TESTCASE_BUF and __AFL_FUZZ_TESTCASE_LEN.

    FORBIDDEN INTERNAL FUNCTIONS:
    NEVER call these even if you see them used inside the target function source — they will fail to link.
    - heap_caps_malloc()
    - heap_caps_free()
    - MALLOC_CAP_SPIRAM
    - MALLOC_CAP_DEFAULT
    - CONFIG_SPIRAM
    - sdkconfig.h
    - esp_heap_caps.h
    - any other symbols that are defined only in the ESP-IDF internal headers.

    PROVEN WORKING TEMPLATE:
    ```c
    #include <stddef.h>
    #include <stdint.h>
    #include <stdio.h>
    #include <string.h>
    #include "PSRAMAllocator.h"

    /* AFL++ persistent mode initialization */
    __AFL_FUZZ_INIT();

    /* Upper bound for allocation to avoid OOM */
    #define MAX_ALLOC_BYTES (1 << 20)  // 1 MiB

    int main(int argc, char **argv) {
        (void)argc; (void)argv;
        __AFL_INIT();
        while (__AFL_LOOP(10000)) {
            const uint8_t *data = __AFL_FUZZ_TESTCASE_BUF;
            size_t   len  = __AFL_FUZZ_TESTCASE_LEN;

            /* The fuzz input is interpreted as a requested allocation size (size_t) */
            if (len < sizeof(size_t)) {
                /* Not enough data to form a size request – skip this iteration */
                continue;
            }
            size_t req_size;
            memcpy(&req_size, data, sizeof(size_t));
            /* Apply safe upper bound */
            if (req_size > MAX_ALLOC_BYTES) {
                req_size = MAX_ALLOC_BYTES;
            }
            if (req_size == 0) {
                continue; // nothing to allocate
            }

            /* Use the PSRAMAllocator to allocate a buffer of bytes */
            PSRAMAllocator<uint8_t> allocator;
            uint8_t *ptr = allocator.allocate(req_size);
            if (!ptr) {
                /* Allocation failed – treat as non‑crash and continue */
                continue;
            }

            /* OPTIONAL: fill the allocated memory with fuzz data (up to min(req_size, len)) */
            size_t copy_len = req_size < len ? req_size : len;
            memcpy(ptr, data, copy_len);

            /* TODO: adapt for target function X – e.g. call a library routine that consumes the buffer */
            // example_function(ptr, req_size);

            /* Clean up */
            allocator.deallocate(ptr, req_size);
        }
        return 0;
    }
    ```

    Output ONLY valid JSON with:
    {
      "target_func": "allocate",
      "input_format": "raw binary where the first sizeof(size_t) bytes specify the allocation size (capped at 1 MiB), remaining bytes are optional payload",
      "c_code": "<the complete C harness source code from the PROVEN WORKING TEMPLATE section>",
      "seed_commands": ["printf '\\x10\\x00\\x00\\x00' > seed1.bin", "dd if=/dev/urandom of=seed2.bin bs=64 count=1"],
      "compile_flags": "-g -O1 -fno-omit-frame-pointer"
    }
  api_func_fixes: {}
  magic_bytes: {}
recon_scoring:
  bonus_patterns: {}
  bonus_func_patterns: {}
  penalty_dirs:
  - test
  - tests
  - doc
  - docs
  - man
  penalty_files: []
  penalty_funcs: []
  low_value_files: {}
seeds:
  formats: {}
  oss_fuzz_corpus: $HOME/Nemesis/seeds/oss_fuzz_corpus_meatloaf
  oss_fuzz_fuzzer_names: []
