# Auto-generated by `nemesis onboard`. Review all paths before running.
# Source scanned: /home/giorg/fdpp_clean
# Build system:   autoconf
# Language:       C++
#
# Oracle expansion candidates (Fix 148-150):
#   TSan candidate:  NO (no pthread/std::thread/OpenMP/_Atomic in source)
#   MSan candidate:  MAYBE (external deps need MSan-rebuild: -lstdc++)
#                    → before enabling, rebuild deps with -fsanitize=memory
#                      or accept a false-positive flood from those libs.
fuzzing:
  timeout_hours: 0.25
target:
  name: fdpp
  oss_fuzz_project: fdpp
  source_root: $HOME/fdpp_clean
  work_root: $HOME/fdpp_work
  build_dir: $HOME/fdpp_work/build_fuzz
  debug_build_dir: $HOME/fdpp_clean/build_debug
  ubsan_build_dir: $HOME/fdpp_clean/build_ubsan
  coverage_build_dir: $HOME/fdpp_clean/build_coverage
  build:
    configure: '{ (cd .. && make distclean) >/dev/null 2>&1; true; }; rm -f ../config.status ../config.log >/dev/null 2>&1 || true; [ -x ../configure ] || (cd .. && (./autogen.sh --help >/dev/null 2>&1 && ./autogen.sh --without-python || autoreconf -fi)) && CC=afl-clang-fast CXX=afl-clang-fast++ CFLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" CXXFLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" ../configure --enable-static --disable-shared --without-python'
    make: make -j$(nproc) -C fdpp libfdpp.la
    debug_configure: '{ (cd .. && make distclean) >/dev/null 2>&1; true; }; rm -f ../config.status ../config.log >/dev/null 2>&1 || true; [ -x ../configure ] || (cd .. && (./autogen.sh --help >/dev/null 2>&1 && ./autogen.sh --without-python || autoreconf -fi)) && CC=clang CXX=clang++ CFLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" CXXFLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" ../configure --enable-static --disable-shared --without-python'
    debug_make: make -j$(nproc) -C fdpp libfdpp.la
    ubsan_configure: '{ (cd .. && make distclean) >/dev/null 2>&1; true; }; rm -f ../config.status ../config.log >/dev/null 2>&1 || true; [ -x ../configure ] || (cd .. && (./autogen.sh --help >/dev/null 2>&1 && ./autogen.sh --without-python || autoreconf -fi)) && CC=clang CXX=clang++ CFLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" CXXFLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" ../configure --enable-static --disable-shared --without-python'
    ubsan_make: make -j$(nproc) -C fdpp libfdpp.la
    coverage_configure: '{ (cd .. && make distclean) >/dev/null 2>&1; true; }; rm -f ../config.status ../config.log >/dev/null 2>&1 || true; [ -x ../configure ] || (cd .. && (./autogen.sh --help >/dev/null 2>&1 && ./autogen.sh --without-python || autoreconf -fi)) && CC=clang CXX=clang++ CFLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" CXXFLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" ../configure --enable-static --disable-shared --without-python'
    coverage_make: make -j$(nproc) -C fdpp libfdpp.la
  repro_binary: ''
  repro_args: []
  source_subdir: fdpp
  include_subdir: include/fdpp
  library_name: .libs/libfdpp.a
  link_libs: -lpthread -lstdc++ -lm
  harness_includes:
  - bprm.h
  harness_template: |
    You are a fuzzing engineer generating AFL++ harnesses for fdpp.

    CRITICAL RULES:
    1. Feed the entire fuzz input as an in‑memory buffer to the library; use __AFL_FUZZ_TESTCASE_BUF and __AFL_FUZZ_TESTCASE_LEN.
    2. The library operates on a struct _bprm instance that must be populated from the buffer; no separate handle/context object is required.
    3. Always allocate a _bprm structure on the stack (or heap) and zero‑initialize it before filling fields. No explicit cleanup function exists, but if you allocate any auxiliary memory you must free it before the next iteration.
    4. NEVER allocate sizes or copy lengths directly from attacker‑controlled values without bounds checks – the _bprm fields such as BprmLen, HeapSize, etc. must be clamped to reasonable limits (e.g. <= 0x10000) to avoid false positives from out‑of‑bounds writes.
    5. Required includes are exactly the public headers listed in "harness_includes" (bprm.h) plus standard headers needed for AFL and memory handling.
    6. AFL++ persistent mode must be used: place __AFL_FUZZ_INIT(); at file scope right after includes, call __AFL_INIT() in main, wrap the test loop with __AFL_LOOP(10000), and use __AFL_FUZZ_TESTCASE_BUF / __AFL_FUZZ_TESTCASE_LEN inside the loop.
    7. Do not call any internal helper functions that are not declared in the public header; such calls will cause link errors.
    8. The harness must return 0 on normal completion of each iteration; any crash or abort will be reported by AFL.
    9. Keep the code strictly C99 compatible; avoid C++ extensions.

    FORBIDDEN INTERNAL FUNCTIONS:
    NEVER call these even if you see them used inside the target function source — they will fail to link.
    - FDPPInternalParse()
    - FDPPLoadSegment()
    - FDPPSetupMemoryMap()
    - FDPPValidateChecksum()
    - FDPPConfigureDevice()
    - FDPPInitKernelHigh()
    - FDPPInitHeapHigh()
    - FDPPInitHeapHMA()
    - FDPPProcessXtra()
    - FDPPFinalize()

    PROVEN WORKING TEMPLATE:
    ```c
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    #include "bprm.h"

    /* AFL++ persistent mode initialization */
    __AFL_FUZZ_INIT();

    /* Helper to safely copy data from the AFL buffer into a _bprm struct */
    static void fill_bprm_from_input(const uint8_t *data, size_t size, struct _bprm *bprm) {
        /* Zero‑initialize the struct */
        memset(bprm, 0, sizeof(*bprm));

        /* Guard against overly large inputs – clamp to struct size */
        size_t copy_len = size < sizeof(*bprm) ? size : sizeof(*bprm);
        memcpy(bprm, data, copy_len);

        /* Apply sane limits to fields that control sizes */
        if (bprm->HeapSize > 0x10000) bprm->HeapSize = 0x10000;
        if (bprm->BprmLen > 0x10000) bprm->BprmLen = 0x10000;
        /* Flags may contain only known bits */
        bprm->Flags &= (FDPP_FL_KERNEL_HIGH | FDPP_FL_HEAP_HIGH | FDPP_FL_HEAP_HMA);
    }

    int main(int argc, char **argv) {
        /* AFL++ entry‑point init (sets up shared memory, etc.) */
        __AFL_INIT();

        while (__AFL_LOOP(10000)) {
            const uint8_t *buf = __AFL_FUZZ_TESTCASE_BUF;
            size_t      len = __AFL_FUZZ_TESTCASE_LEN;

            struct _bprm bprm_instance;
            fill_bprm_from_input(buf, len, &bprm_instance);

            /* TODO: adapt for target function X */
            /* Example usage – pretend there is a public API that processes a _bprm */
            /* If the library exposed a function like `int fdpp_process_bprm(const struct _bprm *p);`
               you would call it here. Since only the struct definition is public, this
               placeholder demonstrates where the real call goes. */
            // int rc = fdpp_process_bprm(&bprm_instance);
            // (ignore rc for now)

            /* No explicit cleanup required for the _bprm struct */
        }
        return 0;
    }
    ```

    Output ONLY valid JSON with:
    {
      "target_func": "fdpp_process_bprm",
      "input_format": "raw binary representing a _bprm structure",
      "c_code": "<the C harness source exactly as shown above>",
      "seed_commands": ["dd if=/dev/zero of=seed.bprm bs=1 count=64", "printf '\\x0a\\x00' >> seed.bprm"],
      "compile_flags": "-g -O1 -fno-omit-frame-pointer"
    }
  api_func_fixes: {}
  magic_bytes: {}
recon_scoring:
  bonus_patterns: {}
  bonus_func_patterns: {}
  penalty_dirs:
  - test
  - tests
  - doc
  - docs
  - man
  penalty_files: []
  penalty_funcs: []
  low_value_files: {}
seeds:
  formats: {}
  oss_fuzz_corpus: $HOME/Nemesis/seeds/oss_fuzz_corpus_fdpp
  oss_fuzz_fuzzer_names: []
