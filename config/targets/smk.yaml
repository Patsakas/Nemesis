# Auto-generated by `nemesis onboard`. Review all paths before running.
# Source scanned: /home/giorg/smk_clean
# Build system:   meson
# Language:       C
#
# Oracle expansion candidates (Fix 148-150):
#   TSan candidate:  NO (no pthread/std::thread/OpenMP/_Atomic in source)
#   MSan candidate:  YES (no external link deps that need MSan-rebuild)
#                    → enable: set sanitizer_profile: msan and
#                      msan_supported: true. Run as a SEPARATE campaign;
#                      mutually exclusive with the default ASAN build.
fuzzing:
  timeout_hours: 0.25
target:
  name: smk
  oss_fuzz_project: smk
  source_root: $HOME/smk_clean
  work_root: $HOME/smk_work
  build_dir: $HOME/smk_work/build_fuzz
  debug_build_dir: $HOME/smk_clean/build_debug
  ubsan_build_dir: $HOME/smk_clean/build_ubsan
  coverage_build_dir: $HOME/smk_clean/build_coverage
  build:
    configure: export CC=afl-clang-fast CXX=afl-clang-fast++ CFLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" CXXFLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" LDFLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations"; meson setup . .. --buildtype=plain --default-library=static -Dwerror=false || meson setup . .. --buildtype=plain --default-library=static -Dwerror=false --wipe
    make: ninja src/libsmk.a || ninja
    debug_configure: export CC=clang CXX=clang++ CFLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" CXXFLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" LDFLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations"; meson setup . .. --buildtype=plain --default-library=static -Dwerror=false || meson setup . .. --buildtype=plain --default-library=static -Dwerror=false --wipe
    debug_make: ninja src/libsmk.a || ninja
    ubsan_configure: export CC=clang CXX=clang++ CFLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" CXXFLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" LDFLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations"; meson setup . .. --buildtype=plain --default-library=static -Dwerror=false || meson setup . .. --buildtype=plain --default-library=static -Dwerror=false --wipe
    ubsan_make: ninja src/libsmk.a || ninja
    coverage_configure: export CC=clang CXX=clang++ CFLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" CXXFLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" LDFLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations"; meson setup . .. --buildtype=plain --default-library=static -Dwerror=false || meson setup . .. --buildtype=plain --default-library=static -Dwerror=false --wipe
    coverage_make: ninja src/libsmk.a || ninja
  repro_binary: ''
  repro_args: []
  source_subdir: src
  include_subdir: src/smk
  library_name: libsmk.a
  link_libs: -lm
  harness_includes:
  - console.h
  harness_template: |+
    You are a fuzzing engineer generating AFL++ harnesses for smk.

    CRITICAL RULES:
    1. Feed the entire fuzz input as a raw byte buffer using the AFL++ globals `__AFL_FUZZ_TESTCASE_BUF` and `__AFL_FUZZ_TESTCASE_LEN`.
    2. The library does not require a handle or context object; its functions operate directly on global state.
    3. There is no explicit init/cleanup API for the console subsystem, but `console_task()` must be called periodically to flush the internal ring buffer after feeding data.
    4. NEVER allocate attacker‑controlled sizes for internal buffers; the console API only accepts single bytes, so avoid calling `malloc` or similar with fuzz‑derived lengths.
    5. Required includes are exactly the public headers listed in `harness_includes` plus `<stddef.h>` for size types.
    6. AFL++ persistent mode must be used: place `__AFL_FUZZ_INIT();` at file scope right after the includes, and wrap the fuzz loop with `__AFL_LOOP(10000)`. Use the macros `__AFL_FUZZ_TESTCASE_BUF` and `__AFL_FUZZ_TESTCASE_LEN` inside the loop.
    7. Do NOT call any internal or private helpers – the smk library has no private headers, so this section is omitted.

    PROVEN WORKING TEMPLATE:
    ```c
    #include <stddef.h>
    #include "console.h"

    /* AFL++ persistent mode initialization – must be at file scope */
    __AFL_FUZZ_INIT();

    static void feed_console(const uint8_t *data, size_t size) {
        for (size_t i = 0; i < size; ++i) {
            console_putc(data[i]);
        }
        /* Flush the queued bytes to the host */
        console_task();
    }

    int main(int argc, char **argv) {
        /* AFL++ entry‑point macro */
        __AFL_INIT();

        while (__AFL_LOOP(10000)) {
            const uint8_t *buf = __AFL_FUZZ_TESTCASE_BUF;
            size_t len = __AFL_FUZZ_TESTCASE_LEN;

            /* Ensure we have at least one byte; empty inputs are ignored */
            if (len == 0) {
                continue;
            }

            /* Exercise the target API */
            feed_console(buf, len);
            /* TODO: adapt for target function X – if additional console APIs are added, invoke them here */
        }
        return 0;
    }
    ```

    Output ONLY valid JSON with:
    {
      "target_func": "function_name",
      "input_format": "smk-specific input description",
      "c_code": "complete C harness source code using the template above",
      "seed_commands": ["shell commands to generate seed files"],
      "compile_flags": "-g -O1 -fno-omit-frame-pointer"
    }

  api_func_fixes: {}
  magic_bytes: {}
recon_scoring:
  bonus_patterns: {}
  bonus_func_patterns:
    putc: 5
  penalty_dirs:
  - test
  - tests
  - doc
  - docs
  - man
  penalty_files: []
  penalty_funcs: []
  low_value_files: {}
seeds:
  formats: {}
  oss_fuzz_corpus: $HOME/Nemesis/seeds/oss_fuzz_corpus_smk
  oss_fuzz_fuzzer_names: []
