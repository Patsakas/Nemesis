# Auto-generated by `nemesis onboard`. Review all paths before running.
# Source scanned: $HOME/re2_clean
# Build system:   cmake
# Language:       C++
#
# External dependencies (CMake find_package):
#   absl               → apt install libabsl-dev
#   ICU                → apt install libicu-dev
#   GTest              → (no apt hint; check distro packages)
#   benchmark          → (no apt hint; check distro packages)
#
#   Bulk install: sudo apt install libabsl-dev libicu-dev
#
# Oracle expansion candidates (Fix 148-150):
#   TSan candidate:  NO (no pthread/std::thread/OpenMP/_Atomic in source)
#   MSan candidate:  MAYBE (external deps need MSan-rebuild: -lstdc++ -labsl_strings -labsl_str_format_internal -labsl_strings_internal -labsl_synchronization -labsl_hash -labsl_city -labsl_low_level_hash -labsl_throw_delegate -labsl_int128 -labsl_raw_logging_internal -labsl_log_severity -labsl_base -labsl_spinlock_wait -labsl_malloc_internal -labsl_time -labsl_time_zone -labsl_civil_time -licuuc -licudata)
#                    → before enabling, rebuild deps with -fsanitize=memory
#                      or accept a false-positive flood from those libs.
fuzzing:
  timeout_hours: 0.25
target:
  name: re2
  oss_fuzz_project: re2
  source_root: $HOME/re2_clean
  work_root: $HOME/re2_work
  build_dir: $HOME/re2_work/build_fuzz
  debug_build_dir: $HOME/re2_clean/build_debug
  ubsan_build_dir: $HOME/re2_clean/build_ubsan
  coverage_build_dir: $HOME/re2_clean/build_coverage
  build:
    configure: rm -f CMakeCache.txt && CC=afl-clang-fast CXX=afl-clang-fast++ cmake .. -DCMAKE_C_COMPILER=afl-clang-fast -DCMAKE_CXX_COMPILER=afl-clang-fast++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DCMAKE_CXX_FLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DRE2_BUILD_FRAMEWORK=OFF -DRE2_BUILD_TESTING=OFF
    make: make -j$(nproc) re2
    debug_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DCMAKE_CXX_FLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DRE2_BUILD_FRAMEWORK=OFF -DRE2_BUILD_TESTING=OFF
    debug_make: make -j$(nproc) re2
    ubsan_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DCMAKE_CXX_FLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DRE2_BUILD_FRAMEWORK=OFF -DRE2_BUILD_TESTING=OFF
    ubsan_make: make -j$(nproc) re2
    coverage_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DCMAKE_CXX_FLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DRE2_BUILD_FRAMEWORK=OFF -DRE2_BUILD_TESTING=OFF
    coverage_make: make -j$(nproc) re2
  repro_binary: ''
  repro_args: []
  source_subdir: ''
  include_subdir: ''
  library_name: libre2.a
  link_libs: -lpthread -lstdc++ -labsl_strings -labsl_str_format_internal -labsl_strings_internal -labsl_synchronization -labsl_hash -labsl_city -labsl_low_level_hash -labsl_throw_delegate -labsl_int128 -labsl_raw_logging_internal -labsl_log_severity -labsl_base -labsl_spinlock_wait -labsl_malloc_internal -labsl_time -labsl_time_zone -labsl_civil_time -licuuc -licudata -lm
  harness_includes:
  - re2/re2.h
  - string
  - string_view
  harness_template: |
    You are a fuzzing engineer generating AFL++ harnesses for re2.

    CRITICAL RULES:
    1. Feed fuzz input as a null-terminated string (char*) via __AFL_FUZZ_TESTCASE_BUF. RE2 operates on C++ std::string/absl::string_view, so the harness must wrap the buffer in a std::string or string_view.
    2. RE2 is a C++ library; harnesses MUST be compiled as C++ (use .cc/.cpp extension). All API calls are in the RE2 namespace.
    3. No persistent context is needed for basic matching. For pre-compiled patterns, create an RE2 object once per __AFL_LOOP iteration (not globally) to avoid state pollution.
    4. Mandatory cleanup: None for RE2 objects (RAII), but avoid memory leaks in harness code (e.g., don’t allocate strings without bounds).
    5. Forbidden: Do NOT use attacker-controlled input to construct regex patterns without validation (ReDoS risk). Always use static/fixed patterns in the harness.
    6. Required includes: <re2/re2.h>, <string>, <string_view> (C++17+).
    7. AFL++ persistent mode: Use __AFL_FUZZ_INIT() at file scope, __AFL_LOOP(10000) in main(), and access input via __AFL_FUZZ_TESTCASE_BUF/__AFL_FUZZ_TESTCASE_LEN.
    8. Never pass __AFL_FUZZ_TESTCASE_BUF directly to RE2 without null-termination or length checks. Use std::string_view(__AFL_FUZZ_TESTCASE_BUF, __AFL_FUZZ_TESTCASE_LEN) for safety.
    9. Compile with C++17 or later (-std=c++17).
    10. Avoid RE2::Set, RE2::DFA, and other advanced features in initial harnesses; focus on FullMatch/PartialMatch/Consume.

    FORBIDDEN INTERNAL FUNCTIONS:
    NEVER call these even if you see them used inside the target function source — they will fail to link.
    `RE2::DFA::Compile()`, `RE2::DFA::Match()`, `RE2::Prog::Compile()`, `Regexp::Parse()`, `Regexp::Simplify()`, `StringPiece::set()`, `StringPiece::assign()`, `Rune::DecodeRune()`, `Rune::EncodeRune()`, `Rune::ValidRune()`.

    PROVEN WORKING TEMPLATE:
    ```c
    #include <re2/re2.h>
    #include <string>
    #include <string_view>

    __AFL_FUZZ_INIT();

    int main(int argc, char **argv) {
      __AFL_INIT();

      __AFL_LOOP(10000) {
        std::string_view input(
            reinterpret_cast<const char*>(__AFL_FUZZ_TESTCASE_BUF),
            __AFL_FUZZ_TESTCASE_LEN);

        // Static pattern: adjust for target function
        const RE2 pattern(R"(hello (\w+) world)");

        // Example: FullMatch (replace with target function)
        std::string match1;
        if (RE2::FullMatch(input, pattern, &match1)) {
          // Valid match; RE2 handles UTF-8 by default
          volatile uint8_t sink = match1.size(); // Prevent dead-store elimination
          (void)sink;
        }

        // TODO: adapt for target function X (e.g., PartialMatch, Consume, etc.)
        // Example alternatives:
        // RE2::PartialMatch(input, pattern, &match1);
        // RE2::Consume(&input, pattern, &match1);
      }

      return 0;
    }
    ```

    Output ONLY valid JSON with:
    { "target_func": "function_name", "input_format": "UTF-8 string (null-terminated or length-delimited)", "c_code": "complete C++ harness source code using the template above", "seed_commands": ["echo 'hello world' > seed1.txt", "echo 'test123' > seed2.txt"], "compile_flags": "-std=c++17 -g -O1 -fno-omit-frame-pointer" }
  api_func_fixes: {}
  magic_bytes: {}
recon_scoring:
  bonus_patterns: {}
  bonus_func_patterns:
    FullMatch: 15
    PartialMatch: 15
    Consume: 12
    FindAndConsume: 12
    Match: 10
    Set::Match: 8
    'DFA::': 5
  penalty_dirs:
  - test
  - tests
  - doc
  - docs
  - man
  penalty_files: []
  penalty_funcs: []
  low_value_files: {}
seeds:
  formats: {}
  oss_fuzz_corpus: $HOME/Nemesis/seeds/oss_fuzz_corpus_re2
  oss_fuzz_fuzzer_names: []
