# Auto-generated by `nemesis onboard`. Review all paths before running.
# Source scanned: /home/giorg/ESCape32_clean
# Build system:   cmake
# Language:       C
#
# Oracle expansion candidates (Fix 148-150):
#   TSan candidate:  NO (no pthread/std::thread/OpenMP/_Atomic in source)
#   MSan candidate:  YES (no external link deps that need MSan-rebuild)
#                    → enable: set sanitizer_profile: msan and
#                      msan_supported: true. Run as a SEPARATE campaign;
#                      mutually exclusive with the default ASAN build.
fuzzing:
  timeout_hours: 0.25
target:
  name: ESCape32
  oss_fuzz_project: ESCape32
  source_root: $HOME/ESCape32_clean
  work_root: $HOME/ESCape32_work
  build_dir: $HOME/ESCape32_work/build_fuzz
  debug_build_dir: $HOME/ESCape32_clean/build_debug
  ubsan_build_dir: $HOME/ESCape32_clean/build_ubsan
  coverage_build_dir: $HOME/ESCape32_clean/build_coverage
  build:
    configure: rm -f CMakeCache.txt && CC=afl-clang-fast CXX=afl-clang-fast++ cmake .. -DCMAKE_C_COMPILER=afl-clang-fast -DCMAKE_CXX_COMPILER=afl-clang-fast++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    make: make -j$(nproc) ESCape32
    debug_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    debug_make: make -j$(nproc) ESCape32
    ubsan_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    ubsan_make: make -j$(nproc) ESCape32
    coverage_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    coverage_make: make -j$(nproc) ESCape32
  repro_binary: ''
  repro_args: []
  source_subdir: src
  include_subdir: src
  library_name: libescape32.a
  link_libs: -lm
  harness_includes:
  - common.h
  harness_template: |
    You are a fuzzing engineer generating AFL++ harnesses for ESCape32.

    CRITICAL RULES:
    1. Feed the entire fuzz input as an in‑memory buffer using the AFL++ globals `__AFL_FUZZ_TESTCASE_BUF` and `__AFL_FUZZ_TESTCASE_LEN`. Do NOT write the data to a temporary file.
    2. The library operates on global state (e.g. `cfg`, `tick`, etc.) and does not require a separate handle/context object. Initialise the library with `init()` before exercising any target API.
    3. Mandatory init/cleanup pairing: call `init()` once at the start of each AFL iteration; there is no explicit shutdown function, but you must reset mutable globals (e.g. `tick = 0;`) if the target function mutates them and you plan to reuse the same process.
    4. Forbidden patterns: never allocate buffers whose size is derived directly from attacker‑controlled input without a hard upper bound (e.g. `malloc(len)` where `len` comes from the fuzz buffer). The library expects fixed‑size structures; allocating variable‑size memory can cause false positives.
    5. Required includes: `#include "common.h"` and any standard headers needed for the harness (`<stddef.h>`, `<stdint.h>`). Do NOT include private/internal headers.
    6. AFL++ persistent mode requirements: place `__AFL_FUZZ_INIT();` at file scope immediately after the includes, then use `__AFL_LOOP(10000)` inside `main`, and access the testcase via `__AFL_FUZZ_TESTCASE_BUF` and `__AFL_FUZZ_TESTCASE_LEN`.

    FORBIDDEN INTERNAL FUNCTIONS:
    NEVER call these even if you see them used inside the target function source — they will fail to link.
    - `__GPIO`
    - `GPIO##port##_##name`
    - `CLK_CNT`
    - `CLK_KHZ`
    - `CLK_MHZ`
    - `IO_ANALOG`
    - `min`
    - `max`
    - `clamp`
    - `NTC10K3455LO2K`

    PROVEN WORKING TEMPLATE:
    ```c
    #include "common.h"
    #include <stddef.h>
    #include <stdint.h>

    /* AFL++ persistent mode initialization */
    __AFL_FUZZ_INIT();

    /* Helper to safely copy fuzz data into a fixed‑size buffer if needed */
    static void copy_input(const uint8_t *src, size_t len) {
        /* The library works on global state; we simply keep the raw buffer
           accessible via globals for any API that expects a pointer/len. */
        (void)src; (void)len; /* no-op – placeholder for future adaptations */
    }

    int main(int argc, char **argv) {
        (void)argc; (void)argv;
        __AFL_INIT();
        while (__AFL_LOOP(10000)) {
            const uint8_t *data = __AFL_FUZZ_TESTCASE_BUF;
            size_t size = __AFL_FUZZ_TESTCASE_LEN;

            /* Reset mutable globals that may have been altered in previous iteration */
            tick = 0;
            throt = brake = ertm = erpm = temp1 = temp2 = volt = curr = csum = dshotval = beepval = 0;
            analog = telreq = telmode = telphid = flipdir = beacon = dshotext = rearm = auxup = 0;

            /* Initialise library state */
            init();

            /* Example target: execute a command string parsed from the fuzz buffer */
            /* The library expects a NUL‑terminated C string; we copy up to size-1 bytes */
            char cmd_buf[256];
            size_t copy_len = size < sizeof(cmd_buf) - 1 ? size : sizeof(cmd_buf) - 1;
            for (size_t i = 0; i < copy_len; ++i) cmd_buf[i] = (char)data[i];
            cmd_buf[copy_len] = '\0';

            /* Call the candidate fuzz target */
            int ret = execcmd(cmd_buf); /* TODO: adapt for target function X */
            (void)ret; /* suppress unused‑result warning */
        }
        return 0;
    }
    ```

    Output ONLY valid JSON with:
    {
      "target_func": "function_name",
      "input_format": "ESCape32‑specific input description",
      "c_code": "complete C harness source code using the template above",
      "seed_commands": ["shell commands to generate seed files"],
      "compile_flags": "-g -O1 -fno-omit-frame-pointer"
    }
  api_func_fixes: {}
  magic_bytes: {}
recon_scoring:
  bonus_patterns: {}
  bonus_func_patterns:
    execcmd: 12
    playmusic: 10
    init: 8
    crc: 7
    scale: 5
  penalty_dirs:
  - test
  - tests
  - doc
  - docs
  - man
  penalty_files: []
  penalty_funcs: []
  low_value_files: {}
seeds:
  formats: {}
  oss_fuzz_corpus: $HOME/Nemesis/seeds/oss_fuzz_corpus_ESCape32
  oss_fuzz_fuzzer_names: []
