# Auto-generated by `nemesis onboard`. Review all paths before running.
# Source scanned: /home/giorg/gvm_libs_clean
# Build system:   cmake
# Language:       C
#
# External dependencies (CMake find_package):
#   Git                → (no apt hint; check distro packages)
#   cgreen             → (no apt hint; check distro packages)
#   SENTRY             → (no apt hint; check distro packages)
#
# Oracle expansion candidates (Fix 148-150):
#   TSan candidate:  YES (threading tokens found in 1 file(s))
#                    - boreas/alivedetection.c
#                    → enable: set sanitizer_profile: tsan,
#                      tsan_supported: true, AND threaded_oracle: true
#                      on at least one pinned_func.
#   MSan candidate:  YES (no external link deps that need MSan-rebuild)
#                    → enable: set sanitizer_profile: msan and
#                      msan_supported: true. Run as a SEPARATE campaign;
#                      mutually exclusive with the default ASAN build.
fuzzing:
  timeout_hours: 0.25
target:
  name: gvm_libs
  oss_fuzz_project: gvm_libs
  source_root: $HOME/gvm_libs_clean
  work_root: $HOME/gvm_libs_work
  build_dir: $HOME/gvm_libs_work/build_fuzz
  debug_build_dir: $HOME/gvm_libs_clean/build_debug
  ubsan_build_dir: $HOME/gvm_libs_clean/build_ubsan
  coverage_build_dir: $HOME/gvm_libs_clean/build_coverage
  build:
    configure: rm -f CMakeCache.txt && CC=afl-clang-fast CXX=afl-clang-fast++ cmake .. -DCMAKE_C_COMPILER=afl-clang-fast -DCMAKE_CXX_COMPILER=afl-clang-fast++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DBUILD_STATIC=ON -DBUILD_TESTS=OFF
    make: make -j$(nproc) gvm_agent_controller_static
    debug_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DBUILD_STATIC=ON -DBUILD_TESTS=OFF
    debug_make: make -j$(nproc) gvm_agent_controller_static
    ubsan_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DBUILD_STATIC=ON -DBUILD_TESTS=OFF
    ubsan_make: make -j$(nproc) gvm_agent_controller_static
    coverage_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF -DBUILD_STATIC=ON -DBUILD_TESTS=OFF
    coverage_make: make -j$(nproc) gvm_agent_controller_static
  repro_binary: ''
  repro_args: []
  source_subdir: agent_controller
  include_subdir: agent_controller
  library_name: agent_controller/libgvm_agent_controller_static.a
  link_libs: -lm
  harness_includes:
  - agent_controller.h
  harness_template: |
    You are a fuzzing engineer generating AFL++ harnesses for gvm_libs.

    CRITICAL RULES:
    1. Feed the fuzz input as an in‑memory buffer using the AFL++ globals `__AFL_FUZZ_TESTCASE_BUF` and `__AFL_FUZZ_TESTCASE_LEN`. Do NOT write the data to a temporary file unless the target API explicitly requires a filename.
    2. Most API functions operate on opaque handle objects (e.g., `agent_controller_connector_t`, `agent_controller_agent_t`). Always create these objects with the library's *_new() constructors, configure them, and free them with the matching *_free() before exiting the loop iteration.
    3. Mandatory init/cleanup pairing: for every `*_new()` call there must be a corresponding `*_free()` in the same loop iteration. Failure to free will leak memory and may cause false positives in long‑running persistent mode.
    4. Forbidden patterns: never allocate a size based on attacker‑controlled data without first validating it against library‑provided limits. For example, do not call `agent_controller_agent_list_new(len)` where `len` comes directly from `__AFL_FUZZ_TESTCASE_LEN` without a sanity check (e.g., cap at 1024).
    5. Required #include list: include the public header `agent_controller.h` and any standard headers needed for the harness (e.g., `<stdio.h>`, `<stdlib.h>`, `<string.h>`). Do NOT include private/internal headers.
    6. AFL++ persistent mode requirements: place `__AFL_FUZZ_INIT();` at file scope immediately after the includes, use `__AFL_LOOP(10000)` in `main`, and access the testcase data only via `__AFL_FUZZ_TESTCASE_BUF` and `__AFL_FUZZ_TESTCASE_LEN` inside the loop.

    PROVEN WORKING TEMPLATE:
    ```c
    #include <stdio.h>
    #include <stdlib.h>
    #include <string.h>
    #include "agent_controller.h"

    /* AFL++ persistent mode initialization – must be at file scope */
    __AFL_FUZZ_INIT();

    static void configure_connector(agent_controller_connector_t conn, const uint8_t *data, size_t size) {
        /* Example: set the host option using a slice of the fuzz buffer */
        const char *host = "127.0.0.1"; /* fallback */
        if (size > 0) {
            /* ensure null‑termination */
            char *tmp = malloc(size + 1);
            memcpy(tmp, data, size);
            tmp[size] = '\0';
            host = tmp;
            agent_controller_connector_builder(conn, AGENT_CONTROLLER_HOST, host);
            free(tmp);
        } else {
            agent_controller_connector_builder(conn, AGENT_CONTROLLER_HOST, host);
        }
        /* Set protocol to http (hard‑coded) */
        agent_controller_connector_builder(conn, AGENT_CONTROLLER_PROTOCOL, "http");
        /* Set port to a safe default */
        int port = 80;
        agent_controller_connector_builder(conn, AGENT_CONTROLLER_PORT, &port);
    }

    int main(int argc, char **argv) {
        __AFL_INIT();

        while (__AFL_LOOP(10000)) {
            const uint8_t *buf = __AFL_FUZZ_TESTCASE_BUF;
            size_t      len = __AFL_FUZZ_TESTCASE_LEN;

            /* Create and configure a connector */
            agent_controller_connector_t conn = agent_controller_connector_new();
            if (!conn) {
                continue; /* skip iteration on allocation failure */
            }
            configure_connector(conn, buf, len);

            /* Example target: fetch the list of agents */
            agent_controller_agent_list_t agents = agent_controller_get_agents(conn);
            if (agents) {
                /* Iterate over returned agents – this exercises parsing of the
                   internal JSON payloads that the server would return. */
                for (int i = 0; i < agents->count; ++i) {
                    agent_controller_agent_t a = agents->agents[i];
                    if (a && a->agent_id) {
                        /* Access a few fields to ensure they are reachable */
                        (void)strlen(a->agent_id);
                        (void)strlen(a->hostname);
                    }
                }
                agent_controller_agent_list_free(agents);
            }

            /* Clean up connector */
            agent_controller_connector_free(conn);
        }
        return 0;
    }
    ```

    Output ONLY valid JSON with:
    {
      "target_func": "agent_controller_get_agents",
      "input_format": "raw binary buffer interpreted by configure_connector (host string slice)",
      "c_code": "<the complete C harness source code from the PROVEN WORKING TEMPLATE section>",
      "seed_commands": ["printf 'examplehost' > seed1.bin", "printf '\x7f\x00\x00\x01' > seed2.bin"],
      "compile_flags": "-g -O1 -fno-omit-frame-pointer"
    }
  api_func_fixes: {}
  magic_bytes: {}
recon_scoring:
  bonus_patterns: {}
  bonus_func_patterns:
    parse_agent_config_string: 12
    download_support_bundle: 12
    get_agents: 10
    update_agents: 10
    get_installer_instruction: 8
  penalty_dirs:
  - test
  - tests
  - doc
  - docs
  - man
  penalty_files: []
  penalty_funcs: []
  low_value_files: {}
seeds:
  formats: {}
  oss_fuzz_corpus: $HOME/Nemesis/seeds/oss_fuzz_corpus_gvm_libs
  oss_fuzz_fuzzer_names: []
