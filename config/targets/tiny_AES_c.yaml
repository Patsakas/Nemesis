# Auto-generated by `nemesis onboard`. Review all paths before running.
# Source scanned: /home/giorg/tiny_AES_c_clean
# Build system:   cmake
# Language:       C++
#
# Oracle expansion candidates (Fix 148-150):
#   TSan candidate:  NO (no pthread/std::thread/OpenMP/_Atomic in source)
#   MSan candidate:  MAYBE (external deps need MSan-rebuild: -lstdc++)
#                    → before enabling, rebuild deps with -fsanitize=memory
#                      or accept a false-positive flood from those libs.
fuzzing:
  timeout_hours: 0.25
target:
  name: tiny_AES_c
  oss_fuzz_project: tiny_AES_c
  source_root: $HOME/tiny_AES_c_clean
  work_root: $HOME/tiny_AES_c_work
  build_dir: $HOME/tiny_AES_c_work/build_fuzz
  debug_build_dir: $HOME/tiny_AES_c_clean/build_debug
  ubsan_build_dir: $HOME/tiny_AES_c_clean/build_ubsan
  coverage_build_dir: $HOME/tiny_AES_c_clean/build_coverage
  build:
    configure: rm -f CMakeCache.txt && CC=afl-clang-fast CXX=afl-clang-fast++ cmake .. -DCMAKE_C_COMPILER=afl-clang-fast -DCMAKE_CXX_COMPILER=afl-clang-fast++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DCMAKE_CXX_FLAGS="-g -fsanitize=address -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    make: make -j$(nproc) tiny_AES_c
    debug_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DCMAKE_CXX_FLAGS="-g -O1 -fsanitize=address,undefined -fno-sanitize=function,alignment -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    debug_make: make -j$(nproc) tiny_AES_c
    ubsan_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DCMAKE_CXX_FLAGS="-g -O1 -fsanitize=undefined -fno-sanitize=function,alignment -fno-sanitize-recover=undefined -fno-omit-frame-pointer -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    ubsan_make: make -j$(nproc) tiny_AES_c
    coverage_configure: rm -f CMakeCache.txt && CC=clang CXX=clang++ cmake .. -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_BUILD_TYPE=Debug -DCMAKE_C_FLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DCMAKE_CXX_FLAGS="-g -O0 -fprofile-instr-generate -fcoverage-mapping -Wno-error -Wno-unused-variable -Wno-unused-parameter -Wno-uninitialized -Wno-deprecated-declarations" -DBUILD_SHARED_LIBS=OFF
    coverage_make: make -j$(nproc) tiny_AES_c
  repro_binary: ''
  repro_args: []
  source_subdir: ''
  include_subdir: ''
  library_name: libtiny_aes_c.a
  link_libs: -lpthread -lstdc++ -lm
  harness_includes:
  - aes.h
  harness_template: |+
    You are a fuzzing engineer generating AFL++ harnesses for tiny_AES_c.

    CRITICAL RULES:
    1. tiny_AES_c operates on in-memory buffers only; never use FILE* or disk I/O.
    2. All API calls require a valid AES_ctx handle initialized with AES_init_ctx or AES_init_ctx_iv.
    3. Mandatory init/cleanup pairing: AES_init_ctx → AES_*_encrypt/AES_*_decrypt → no explicit cleanup (stack-allocated ctx).
    4. Forbidden: passing attacker-controlled lengths without internal bounds checks; tiny_AES_c expects lengths that are multiples of AES_BLOCKLEN (16) for CBC/CTR and exactly AES_BLOCKLEN for ECB.
    5. Required includes: <stdint.h>, <stddef.h>, "aes.h".
    6. Use AFL++ persistent mode: __AFL_FUZZ_INIT() at file scope, __AFL_LOOP(10000), __AFL_FUZZ_TESTCASE_BUF, __AFL_FUZZ_TESTCASE_LEN.
    7. Never call internal helpers like AES_XOR_encrypt(), AES_ShiftRows(), AES_MixColumns(), AES_AddRoundKey(), AES_KeyExpansion() — they are not declared in public headers and will fail to link.
    8. For CBC/CTR modes, the IV must be set via AES_init_ctx_iv() or AES_ctx_set_iv(); reusing IVs with the same key is insecure but allowed by the API.
    9. Buffer alignment: ensure input/output buffers are 16-byte aligned for best performance (use aligned_alloc or compiler-specific attributes if needed).
    10. Disable ECB if not targeting it; keep CBC/CTR as primary fuzzing targets due to IV misuse risks.

    FORBIDDEN INTERNAL FUNCTIONS:
    - AES_XOR_encrypt()
    - AES_ShiftRows()
    - AES_MixColumns()
    - AES_AddRoundKey()
    - AES_KeyExpansion()
    - AES_EncryptBlock()
    - AES_DecryptBlock()
    - AES_ExpandKey256()
    - AES_ExpandKey192()
    NEVER call these even if you see them used inside the target function source — they will fail to link.

    PROVEN WORKING TEMPLATE:
    ```c
    #include <stdint.h>
    #include <stddef.h>
    #include "aes.h"

    __AFL_FUZZ_INIT();

    int main(int argc, char **argv) {
        __AFL_INIT();

        while (__AFL_LOOP(10000)) {
            size_t len = __AFL_FUZZ_TESTCASE_LEN;
            if (len < AES_BLOCKLEN) continue; // tiny_AES_c requires at least one block

            // Allocate aligned buffers to avoid UB on some platforms
            uint8_t *buf = aligned_alloc(16, len);
            uint8_t key[AES_KEYLEN] = {0};
            struct AES_ctx ctx;

            // Copy fuzz input into buffer
            memcpy(buf, __AFL_FUZZ_TESTCASE_BUF, len);

            // Initialize key (fixed pattern to keep harness deterministic; vary via seeds)
            for (size_t i = 0; i < AES_KEYLEN; i++) key[i] = (uint8_t)(i + 1);

            // Initialize context (use ECB mode for simplicity; switch to CBC/CTR by redefining macros in build flags)
            AES_init_ctx(&ctx, key);

            // TODO: adapt for target function X — replace with AES_CBC_encrypt_buffer, AES_CTR_xcrypt_buffer, or AES_ECB_encrypt
            AES_ECB_encrypt(&ctx, buf);

            // Optional: decrypt to verify round-trip if needed
            // AES_ECB_decrypt(&ctx, buf);

            // Ensure output is used (compiler barrier to prevent optimization)
            asm volatile("" : : "r"(buf[0]) : "memory");

            free(buf);
        }
        return 0;
    }
    ```

    Output ONLY valid JSON with:
    {
      "target_func": "function_name",
      "input_format": "tiny_AES_c-specific input description",
      "c_code": "complete C harness source code using the template above",
      "seed_commands": ["shell commands to generate seed files"],
      "compile_flags": "-g -O1 -fno-omit-frame-pointer"
    }

  api_func_fixes: {}
  magic_bytes: {}
recon_scoring:
  bonus_patterns: {}
  bonus_func_patterns:
    AES_: 12
    encrypt: 10
    decrypt: 10
    ctx_: 8
  penalty_dirs:
  - test
  - tests
  - doc
  - docs
  - man
  penalty_files: []
  penalty_funcs: []
  low_value_files: {}
seeds:
  formats: {}
  oss_fuzz_corpus: $HOME/Nemesis/seeds/oss_fuzz_corpus_tiny_AES_c
  oss_fuzz_fuzzer_names: []
